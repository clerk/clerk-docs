{/*
  Single source of truth for the middleware-based auth recommendation notice.
  Include it wherever the docs show auth checks inside the `clerkMiddleware()`
  callback or `createRouteMatcher()`:

    <Include src="_partials/middleware-auth-deprecation" />

  Edit the wording ONLY here so every page stays in sync. Keep it framework-neutral
  so it can be shared across the Next.js, Nuxt, and Astro pages.
*/}

> [!IMPORTANT]
> Auth checks inside `clerkMiddleware()` are no longer recommended. **Protect access as close to the resource as possible**, in the page, Route Handler, Server Action, or other server-side code that reads or mutates the data. See [the guide on protecting content from unauthenticated users](/docs/guides/secure/protect-content#migrating-from-middleware-based-auth-checks).
>
> `clerkMiddleware()` is still required as it's required for Clerk configuration and can still be used for things like redirects, headers, and other request handling.
