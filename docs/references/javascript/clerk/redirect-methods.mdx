---
title: Redirect Methods
description: These are all methods on the Clerk class that help you manage route navigation based on the Clerk dashboard configuration.
---

import { Tab, Tabs } from "nextra-theme-docs";
import { Tables } from "@/components/Table";
import { Images } from "@/components/Images";
import { CodeBlockTabs } from "@/components/CodeBlockTabs";

# Redirect Methods

These are all methods on [the Clerk class](/docs/references/javascript/clerk/clerk) that help you manage route navigation based on the Clerk dashboard configuration.

## `navigate()`

```typescript
function navigate(to: string | undefined): Promise<unknown>;
```

Helper method which will use the custom push navigation function of your application to navigate to the provided URL or relative path. See the relevant section on routing for more information on navigation.

### Props

<Tables
  headings={["Name", "Type", "Description"]}
  headingsMeta={[{maxWidth:'300px'},{maxWidth: '300px'},{maxWidth: '300px'}]}
  rows={[
  {
  cells: [
  <code>to</code>,
  <code>string | undefined</code>,
  "The route to navigate to",
  ],
  }
  ]}
/>

### Returns

<Tables
  headings={["Type", "Description"]}
  headingsMeta={[{}, {maxWidth: "300px"}]}
  rows={[
  {cells: [
  <code>Promise{"<"}unknown{">"}</code>,
  <>A promise that can be <code>await</code>ed in order to listen for the navigation to finish.<br/>The inner value should not be relied on, as it can change based on the framework it's used within.</>,
  ]},
  ]}
/>

## `redirectWithAuth()`

```typescript
function redirectWithAuth(to: string): Promise<unknown>;
```

Redirects to the provided url after decorating it with the auth token for development instances.


### Props

<Tables
  headings={["Name", "Type", "Description"]}
  headingsMeta={[{maxWidth:'300px'},{maxWidth: '300px'},{maxWidth: '300px'}]}
  rows={[
  {
  cells: [
  <code>to</code>,
  <code>string | undefined</code>,
  "The route to navigate to",
  ],
  }
  ]}
/>

### Returns

<Tables
  headings={["Type", "Description"]}
  headingsMeta={[{}, {maxWidth: "300px"}]}
  rows={[
  {cells: [
  <code>Promise{"<"}unknown{">"}</code>,
  <>A promise that can be <code>await</code>ed in order to listen for the navigation to finish.<br/>The inner value should not be relied on, as it can change based on the framework it's used within.</>,
  ]},
  ]}
/>

## `redirectToSignIn()`

```typescript
function redirectToSignIn(options?: RedirectOptions): Promise<unknown>;
```

Redirects to the sign-in URL, as configured in your Instance settings. This method uses the [`navigate()`](#navigate) method under the hood.


### Props

<Tables
  headings={["Name", "Type", "Description"]}
  headingsMeta={[{maxWidth:'300px'},{maxWidth: '300px'},{maxWidth: '300px'}]}
  rows={[
  {
  cells: [
  <code>options</code>,
  <code><a href="/docs/references/javascript/types/redirect-options">RedirectOptions</a> | undefined</code>,
  "Options to use in the redirect",
  ],
  }
  ]}
/>

### Returns

<Tables
  headings={["Type", "Description"]}
  headingsMeta={[{}, {maxWidth: "300px"}]}
  rows={[
  {cells: [
  <code>Promise{"<"}unknown{">"}</code>,
  <>A promise that can be <code>await</code>ed in order to listen for the navigation to finish.<br/>The inner value should not be relied on, as it can change based on the framework it's used within.</>,
  ]},
  ]}
/>

## `redirectToSignUp()`

```typescript
function redirectToSignUp(options?: RedirectOptions): Promise<unknown>;
```

Redirects to the sign-up URL, as configured in your Instance settings. This method uses the [`navigate()`](#navigate) method under the hood.

### Props

<Tables
  headings={["Name", "Type", "Description"]}
  headingsMeta={[{maxWidth:'300px'},{maxWidth: '300px'},{maxWidth: '300px'}]}
  rows={[
  {
  cells: [
  <code>options</code>,
  <code><a href="/docs/references/javascript/types/redirect-options">RedirectOptions</a> | undefined</code>,
  "Options to use in the redirect",
  ],
  }
  ]}
/>

### Returns

<Tables
  headings={["Type", "Description"]}
  headingsMeta={[{}, {maxWidth: "300px"}]}
  rows={[
  {cells: [
  <code>Promise{"<"}unknown{">"}</code>,
  <>A promise that can be <code>await</code>ed in order to listen for the navigation to finish.<br/>The inner value should not be relied on, as it can change based on the framework it's used within.</>,
  ]},
  ]}
/>


## `redirectToUserProfile()`

```typescript
function redirectToUserProfile(): Promise<unknown>;
```

Redirects to the user profile management URL, as configured in your Instance settings. This method uses the [`navigate()`](#navigate) method under the hood.

### Returns

<Tables
  headings={["Type", "Description"]}
  headingsMeta={[{}, {maxWidth: "300px"}]}
  rows={[
  {cells: [
  <code>Promise{"<"}unknown{">"}</code>,
  <>A promise that can be <code>await</code>ed in order to listen for the navigation to finish.<br/>The inner value should not be relied on, as it can change based on the framework it's used within.</>,
  ]},
  ]}
/>

## `redirectToCreateOrganization()`

```typescript
function redirectToCreateOrganization(): Promise<unknown>;
```

Redirects to the configured URL where <code><a href="/docs/references/javascript/clerk/create-organization">{"<CreateOrganization />"}</a></code> is mounted. This method uses the [`navigate()`](#navigate) method under the hood.

### Returns

<Tables
  headings={["Type", "Description"]}
  headingsMeta={[{}, {maxWidth: "300px"}]}
  rows={[
  {cells: [
  <code>Promise{"<"}unknown{">"}</code>,
  <>A promise that can be <code>await</code>ed in order to listen for the navigation to finish.<br/>The inner value should not be relied on, as it can change based on the framework it's used within.</>,
  ]},
  ]}
/>

## `redirectToOrganizationProfile()`

```typescript
function redirectToOrganizationProfile(): Promise<unknown>;
```

Redirects to the configured URL where <code><a href="/docs/references/javascript/clerk/organization-profile">{"<OrganizationProfile />"}</a></code> is mounted. This method uses the [`navigate()`](#navigate) method under the hood.

### Returns

<Tables
  headings={["Type", "Description"]}
  headingsMeta={[{}, {maxWidth: "300px"}]}
  rows={[
  {cells: [
  <code>Promise{"<"}unknown{">"}</code>,
  <>A promise that can be <code>await</code>ed in order to listen for the navigation to finish.<br/>The inner value should not be relied on, as it can change based on the framework it's used within.</>,
  ]},
  ]}
/>


## `redirectToHome()`

```typescript
function redirectToHome(): Promise<unknown>;
```

Redirects to your app's homepage, as configured in your Instance settings. This method uses the [`navigate()`](#navigate) method under the hood.

### Returns

<Tables
  headings={["Type", "Description"]}
  headingsMeta={[{}, {maxWidth: "300px"}]}
  rows={[
  {cells: [
  <code>Promise{"<"}unknown{">"}</code>,
  <>A promise that can be <code>await</code>ed in order to listen for the navigation to finish.<br/>The inner value should not be relied on, as it can change based on the framework it's used within.</>,
  ]},
  ]}
/>
