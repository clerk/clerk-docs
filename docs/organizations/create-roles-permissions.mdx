---
title: Create custom roles and assign permissions with Clerk
description: 
---

# Create custom roles and assign permissions with Clerk

## Outcomes

- Create a custom role for your organization
- Assign custom permissions to that role
- Use those permissions to control editorial permissions on the content (CRUD) using backend logic
- Changing a user’s role in an org

## Note

You can update members ship both from the dashboard (admin/developer/admin of org view), and also from the organization profile (end user view).