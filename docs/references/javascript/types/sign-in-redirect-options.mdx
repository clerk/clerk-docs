---
title: SignInRedirectOptions
description: An interface that provides options for a range of redirect methods.
---

# `SignInRedirectOptions`

An interface that provides options for sign-in redirect methods.

## Attributes

<Tables
  headings={["Name", "Type", "Description"]}
  headingsMeta={[{maxWidth:'300px'},{maxWidth: '300px'},{maxWidth: '300px'}]}
  rows={[
    {cells: [
        <code>afterSignInUrl</code>,
        <code>string | null | undefined</code>,
        "Full URL or path to navigate after successful sign in."
        ]},
    {cells: [
        <code>afterSignUpUrl</code>,
        <code>string | null | undefined</code>,
        <>Full URL or path to navigate after successful sign up. Sets the <code>afterSignUpUrl</code> if the "Sign up" link is clicked.</>
        ]},
    {cells: [
        <code>redirectUrl</code>,
        <code>string | null | undefined</code>,
        <>Full URL or path to navigate after successful sign in, or sign up.<br/>The same as setting <code>afterSignInUrl</code> and <code>afterSignUpUrl</code>
to the same value.</>
        ]},
    {cells: [
        <code>initialValues</code>,
        <code>SignInInitialValues</code>,
        <>The values used to prefill the sign-in fields with.</>,
      ]},
    ]}
/>