---
title: Clerk types
description: Explore the different types available for typing your application.
sdk: js-frontend, astro, chrome-extension, expo, nextjs, react, react-router, tanstack-react-start, nuxt, vue
---

Types are a powerful tool for adding type-safety to your application. They can help you catch bugs early, make your code easier to understand, and make your code easier to refactor. Clerk provides a number of types to help you add type-safety to your application.

<If sdk="js-frontend">
  To get access to Clerk types, you need to add the `@clerk/shared` package to your project. Run the following command to install it:

  ```npm
  npm install @clerk/shared
  ```

  Then, import the types from the `@clerk/shared/types` entrypoint using a type-only import:

  ```ts
  import type { UserResource } from '@clerk/shared/types'
  ```
</If>

<If notSdk="js-frontend">
  Import types from your SDK's public `/types` entry point using a type-only import.

  <If sdk="astro">
    ```ts
    import type { UserResource } from '@clerk/astro/types'
    ```
  </If>

  <If sdk="chrome-extension">
    ```ts
    import type { UserResource } from '@clerk/chrome-extension/types'
    ```
  </If>

  <If sdk="expo">
    ```ts
    import type { UserResource } from '@clerk/expo/types'
    ```
  </If>

  <If sdk="nextjs">
    ```ts
    import type { UserResource } from '@clerk/nextjs/types'
    ```
  </If>

  <If sdk="react">
    ```ts
    import type { UserResource } from '@clerk/react/types'
    ```
  </If>

  <If sdk="react-router">
    ```ts
    import type { UserResource } from '@clerk/react-router/types'
    ```
  </If>

  <If sdk="tanstack-react-start">
    ```ts
    import type { UserResource } from '@clerk/tanstack-react-start/types'
    ```
  </If>

  <If sdk="nuxt">
    ```ts
    import type { UserResource } from '@clerk/nuxt/types'
    ```
  </If>

  <If sdk="vue">
    ```ts
    import type { UserResource } from '@clerk/vue/types'
    ```
  </If>
</If>
