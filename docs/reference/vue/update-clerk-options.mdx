---
title: '`updateClerkOptions()`'
description: The `updateClerkOptions()` function allows you to update Clerk's options at runtime.
sdk: vue
---

The `updateClerkOptions()` function is used to update Clerk's options at runtime. It can be called at any time after [Clerk has been initialized](/docs/reference/vue/clerk-plugin).

## Usage

```vue
<script setup>
import { updateClerkOptions } from '@clerk/vue'
import { dark } from '@clerk/themes'

const isDark = ref(false)

function toggleTheme() {
  isDark.value = !isDark.value
  updateClerkOptions({
    appearance: {
      theme: isDark.value ? dark : undefined,
    },
  })
}
</script>

<template>
  <button @click="toggleTheme">Toggle Theme</button>
</template>
```

## Properties

<Include src="_partials/update-clerk-options-properties" />
