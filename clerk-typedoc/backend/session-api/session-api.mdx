## createSession()

Creates a new session for the given user.

### Parameters

| Parameter | Type                                                  |
| --------- | ----------------------------------------------------- |
| `params`  | [`CreateSessionParams`](../create-session-params.mdx) |

### Returns

`Promise`\<[`Session`](/docs/reference/backend/types/backend-session)\> — The created [`Session`](/docs/reference/backend/types/backend-session).

---

## getSession()

Gets the given [`Session`](/docs/reference/backend/types/backend-session).

### Parameters

| Parameter   | Type     | Description                   |
| ----------- | -------- | ----------------------------- |
| `sessionId` | `string` | The ID of the session to get. |

### Returns

`Promise`\<[`Session`](/docs/reference/backend/types/backend-session)\>

---

## getSessionList()

Gets a list of sessions for either the specified client or user. Requires either `clientId` or `userId` to be provided. By default, the list is returned in descending order by creation date (newest first).

### Parameters

| Parameter          | Type                                                    | Description                                                                                                                                                                            |
| ------------------ | ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `params`           | [`SessionListParams`](../session-list-params.mdx)       | -                                                                                                                                                                                      |
| `params.clientId?` | `string`                                                | The ID of the client to get sessions for.                                                                                                                                              |
| `params.limit?`    | `number`                                                | Maximum number of items returned per request. Must be an integer greater than zero and less than `501`. Can be used for paginating the results together with offset. Defaults to `10`. |
| `params.offset?`   | `number`                                                | Skip the first `offset` items when paginating. Needs to be an integer greater or equal to zero. To be used in conjunction with `limit`. Defaults to `0`.                               |
| `params.status?`   | [`SessionStatus`](/docs/reference/types/session-status) | The status of the sessions to get.                                                                                                                                                     |
| `params.userId?`   | `string`                                                | The ID of the user to get sessions for.                                                                                                                                                |

### Returns

`Promise`\<[`PaginatedResourceResponse`](../paginated-resource-response.mdx)\<<code>[Session](/docs/reference/backend/types/backend-session)[]</code>\>\> — A [`PaginatedResourceResponse`](/docs/reference/backend/types/paginated-resource-response) object with a `data` property containing an array of [`Session`](/docs/reference/backend/types/backend-session) objects and a `totalCount` property containing the total number of sessions.

---

## getToken()

Gets a session token or generates a JWT using a specified template that is defined in the [**JWT templates**](https://dashboard.clerk.com/~/jwt-templates) page in the Clerk Dashboard.

### Parameters

| Parameter           | Type     | Description                                                                                  |
| ------------------- | -------- | -------------------------------------------------------------------------------------------- |
| `sessionId`         | `string` | The ID of the session to get the token for.                                                  |
| `template?`         | `string` | The name of the JWT template configured in the Clerk Dashboard to generate a new token from. |
| `expiresInSeconds?` | `number` | The expiration time for the token in seconds. If not provided, uses the default expiration.  |

### Returns

`Promise`\<`Token`\> — The generated token.

---

## refreshSession()

### Call Signature

Refreshes the given session.

#### Parameters

| Parameter   | Type                                                                                                                                                                                                                                                                    | Description                            |
| ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| `sessionId` | `string`                                                                                                                                                                                                                                                                | The ID of the session to refresh.      |
| `params`    | <code>\{ expired_token: string; format?: "cookie" \| "token"; refresh_token: string; request_headers?: Record\<string, string[]\>; request_origin: string; request_originating_ip?: string; suffixed_cookies?: boolean; \}</code> & <code>\{ format: "token"; \}</code> | The parameters to refresh the session. |

#### Returns

`Promise`\<`Token`\> — The refreshed token.

### Call Signature

Refreshes the given session.

#### Parameters

| Parameter   | Type                                                                                                                                                                                                                                                                     | Description                            |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------- |
| `sessionId` | `string`                                                                                                                                                                                                                                                                 | The ID of the session to refresh.      |
| `params`    | <code>\{ expired_token: string; format?: "cookie" \| "token"; refresh_token: string; request_headers?: Record\<string, string[]\>; request_origin: string; request_originating_ip?: string; suffixed_cookies?: boolean; \}</code> & <code>\{ format: "cookie"; \}</code> | The parameters to refresh the session. |

#### Returns

`Promise`\<`Cookies`\> — The refreshed token.

### Call Signature

Refreshes the given session.

#### Parameters

| Parameter                        | Type                                                                                                                                                                                                                              | Description                                |
| -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| `sessionId`                      | `string`                                                                                                                                                                                                                          | The ID of the session to refresh.          |
| `params`                         | <code>\{ expired_token: string; format?: "cookie" \| "token"; refresh_token: string; request_headers?: Record\<string, string[]\>; request_origin: string; request_originating_ip?: string; suffixed_cookies?: boolean; \}</code> | The parameters to refresh the session.     |
| `params.expired_token`           | `string`                                                                                                                                                                                                                          | The expired token to refresh.              |
| `params.format?`                 | <code>"cookie" \| "token"</code>                                                                                                                                                                                                  | The format of the token to refresh.        |
| `params.refresh_token`           | `string`                                                                                                                                                                                                                          | The refresh token to refresh.              |
| `params.request_headers?`        | `Record`\<`string`, <code>string[]</code>\>                                                                                                                                                                                       | The headers of the request.                |
| `params.request_origin`          | `string`                                                                                                                                                                                                                          | The origin of the request.                 |
| `params.request_originating_ip?` | `string`                                                                                                                                                                                                                          | The originating IP address of the request. |
| `params.suffixed_cookies?`       | `boolean`                                                                                                                                                                                                                         | Whether to use suffixed cookies.           |

#### Returns

`Promise`\<`Token`\> — The refreshed token.

---

## revokeSession()

Revokes the given session. The user will be signed out from the client the session is associated with.

### Parameters

| Parameter   | Type     | Description                      |
| ----------- | -------- | -------------------------------- |
| `sessionId` | `string` | The ID of the session to revoke. |

### Returns

`Promise`\<[`Session`](/docs/reference/backend/types/backend-session)\> — The revoked [`Session`](/docs/reference/backend/types/backend-session).
