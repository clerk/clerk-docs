import { Tab, Tabs } from "nextra-theme-docs";
import { Tables } from "@/components/Table";
import { Images } from "@/components/Images";
import { CodeBlockTabs } from "@/components/CodeBlockTabs";

# `Verification`


<Tables
  headings={["Name", "Type", "Description"]}
  headingsMeta={[{}, {}, {maxWidth: "300px"}]}
  rows={[
    {cells: [<code>attempts</code>, <code>number | null</code>, <></>]},
    {cells: [<code>error</code>, <code>ClerkAPIError | null</code>, <></>]},
    {cells: [<code>expireAt</code>, <code>Date | null</code>, <></>]},
    {cells: [<code>externalVerificationRedirectURL</code>, <code>URL | null</code>, <></>]},
    {cells: [<code>nonce</code>, <code>string | null</code>, <></>]},
    {cells: [<code>status</code>, <code>VerificationStatus | null</code>, <></>]},
    {cells: [<code>strategy</code>, <code>string | null</code>, <></>]},
    {cells: [<code>verifiedAtClient</code>, <code>string | null</code>, <></>]}
  ]}
/>

## Methods

### `verifiedFromTheSameClient()`

```typescript
function verifiedFromTheSameClient(): boolean;
```