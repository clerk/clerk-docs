import { Tab, Tabs } from "nextra-theme-docs";
import { Tables } from "@/components/Table";

# `<CreateOrganization />`

---

![Organization Switcher Example](https://clerk.dev/_next/image?url=https%3A%2F%2Fcdn.sanity.io%2Fimages%2Fe1ql88v4%2Fproduction%2F99d98eca0c788a208682138febeaab919b0fbd4b-992x568.svg%3Ffit%3Dmax%26auto%3Dformat&w=2048&q=75)

---

The `<CreateOrganization/>` component is used to render an organization creation UI that allows users to create brand new organizations within you application.

## Usage

```jsx copy filename="/pages/create-organization/[[...index].[jsx/tsx]"
import { CreateOrganization } from "@clerk/nextjs";

export default function CreateOrganizationPage() {
  return <CreateOrganization />;
}
```

## Props

<Tables
  headings={["Name", "Type", "Description"]}
  rows={[
    {
      cells: [
        "afterCreateOrganizationUrl?",
        "string",
        "Full URL or path to navigate after creating a new organization.",
      ],
    },
    {
      cells: [
        "path",
        "string",
        "The path where the component is mounted when path-based routing is used. -e.g. /create-org. This prop is ignored in hash and virtual based routing.",
      ],
    },
    {
      cells: [
        "routing",
        "RoutingStrategy",
        `The routing strategy for your pages. Supported values are:
- hash (default): Hash based routing.
- path: Path based routing.
- virtual: Virtual based routing.`,
      ],
    },
    {
      cells: [
        "appearance",
        "object",
        "Control the look and feel of the component.",
      ],
    },
  ]}
/>
