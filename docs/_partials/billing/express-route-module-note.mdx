> [!NOTE]
> This is a route module. It assumes you've applied [`clerkMiddleware()`](/docs/reference/express/clerk-middleware) to all routes in your app, which is what makes the request's authentication state available to `getAuth()`.
