> [!IMPORTANT]
> When using native components, pass `{ treatPendingAsSignedOut: false }` to `useAuth()`. This keeps the auth state in sync with the native SDK and helps avoid "You're already signed in" errors when users have pending [session tasks](!session-tasks).
