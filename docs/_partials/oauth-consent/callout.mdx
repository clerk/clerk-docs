> [!WARNING]
> Custom OAuth consent routes are not generally available yet and must be enabled for your Clerk instance. Pages that host OAuth consent must also set the referrer policy to `strict-origin-when-cross-origin` so the cross-origin POST to FAPI includes the `Origin` header and Clerk can validate the CSRF token.
