---
title: '`list()`'
description: Use the list() method to list the domains for the instance.
---

<Typedoc src="backend/domain-api/methods/list" />

## Example

<Include src="_partials/backend/usage" />

```tsx
const response = await clerkClient.domains.list()
```

## Backend API (BAPI) endpoint

This method in the SDK is a wrapper around the BAPI endpoint `GET/domains`. See the [BAPI reference](/docs/reference/backend-api/tag/domains/GET/domains){{ target: '_blank' }} for more information.
