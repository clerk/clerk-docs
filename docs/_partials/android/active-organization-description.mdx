Use `Clerk.auth.setActive()` to switch the [Active Organization](!active-organization) for a session. Pass `sessionId` and `organizationId` to select an organization, or omit `organizationId` (or pass `null`) to select the user's [Personal Account](!personal-account). The latter isn't allowed when `Clerk.organizationSelectionIsForced` is `true`.
