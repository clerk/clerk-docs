---
title: '`Billing` object'
description: The Billing object provides methods for managing billing for a user or organization.
sdk: nextjs, astro, chrome-extension, expo, expressjs, js-frontend, fastify, nuxt, react, react-router, tanstack-react-start, vue
---

<Include src="_partials/billing/billing-experimental" />

The `Billing` object provides methods for managing billing for a user or organization.

> [!NOTE]
> If an `orgId` parameter is not provided, the methods will automatically use the current user's ID.

## Methods

### `getPaymentAttempt()`

Returns details of a specific payment attempt for the current user or supplied Organization. Returns a [`BillingPaymentResource`](/docs/reference/types/billing-payment-resource) object.

```ts
function getPaymentAttempt(params: GetPaymentAttemptParams): Promise<BillingPaymentResource>
```

#### `GetPaymentAttemptParams`

<Properties>
  - `id`
  - `string`

  The ID of the payment attempt to fetch.

  ---

  - `orgId?`
  - `string`

  The Organization ID to perform the request on.
</Properties>

#### Example

<If sdk="js-frontend">
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object.

  ```js {{ filename: 'src/main.js' }}
  import { Clerk } from '@clerk/clerk-js'

  const publishableKey = import.meta.env.VITE_CLERK_PUBLISHABLE_KEY

  // Initialize Clerk with your Clerk Publishable Key
  const clerk = new Clerk(publishableKey)

  // Load Clerk
  await clerk.load()

  // Call the getPaymentAttempt() method
  await clerk.billing.getPaymentAttempt({
    id: 'payment_attempt_123',
  })
  ```
</If>

<If notSdk={["js-frontend", "astro", "vue", "nuxt"]}>
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`useClerk()`](/docs/reference/hooks/use-clerk) hook to access the `clerk.billing` object, and then call the `getPaymentAttempt()` method.

  <If sdk="react">
    ```tsx {{ filename: 'src/pages/Example.tsx' }}
    import { useClerk } from '@clerk/react'

    export default async function Example() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPaymentAttempt() method
      const paymentAttempt = await clerk.billing.getPaymentAttempt({
        id: 'payment_attempt_123',
      })

      return <pre>{JSON.stringify(paymentAttempt, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="nextjs">
    ```tsx {{ filename: 'app/page.tsx' }}
    'use client'

    import { useClerk } from '@clerk/nextjs'

    export default async function Page() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPaymentAttempt() method
      const paymentAttempt = await clerk.billing.getPaymentAttempt({
        id: 'payment_attempt_123',
      })

      return <pre>{JSON.stringify(paymentAttempt, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="react-router">
    ```tsx {{ filename: 'app/routes/home.tsx' }}
    import { useClerk } from '@clerk/react-router'

    export default async function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPaymentAttempt() method
      const paymentAttempt = await clerk.billing.getPaymentAttempt({
        id: 'payment_attempt_123',
      })

      return <pre>{JSON.stringify(paymentAttempt, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="chrome-extension">
    ```tsx {{ filename: 'src/routes/page.tsx' }}
    import { useClerk } from '@clerk/chrome-extension'

    export default async function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPaymentAttempt() method
      const paymentAttempt = await clerk.billing.getPaymentAttempt({
        id: 'payment_attempt_123',
      })

      return <pre>{JSON.stringify(paymentAttempt, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="tanstack-react-start">
    ```tsx {{ filename: 'app/routes/index.tsx' }}
    import { useClerk } from '@clerk/tanstack-react-start'
    import { createFileRoute } from '@tanstack/react-router'

    export const Route = createFileRoute('/')({
      component: Home,
    })

    export default function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPaymentAttempt() method
      const paymentAttempt = await clerk.billing.getPaymentAttempt({
        id: 'payment_attempt_123',
      })

      return <pre>{JSON.stringify(paymentAttempt, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="expo">
    ```tsx {{ filename: 'app/(clerk)/index.tsx' }}
    import { useClerk } from '@clerk/expo'
    import { Text, View } from 'react-native'

    export default async function Page() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPaymentAttempt() method
      const paymentAttempt = await clerk.billing.getPaymentAttempt({
        id: 'payment_attempt_123',
      })

      return (
        <View>
          <Text style={{ whiteSpace: 'pre' }}>{JSON.stringify(paymentAttempt, null, 2)}</Text>
        </View>
      )
    }
    ```
  </If>
</If>

<If sdk="astro">
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`$clerkStore`](/docs/reference/astro/client-side-helpers/clerk-store) to access the `Clerk` object, and then call the `getPaymentAttempt()` method.

  <CodeBlockTabs options={['React', 'Vue', 'Svelte']}>
    ```tsx {{ filename: 'components/payment-attempt.tsx' }}
    import { useStore } from '@nanostores/react'
    import { $clerkStore } from '@clerk/astro/client'

    export default async function Client() {
      // Use the $clerkStore to access the Clerk object
      const clerk = useStore($clerkStore)

      // Call the getPaymentAttempt() method
      const paymentAttempt = await clerk.billing.getPaymentAttempt({
        id: 'payment_attempt_123',
      })

      return <pre>{JSON.stringify(paymentAttempt, null, 2)}</pre>
    }
    ```

    ```vue {{ filename: 'components/payment-attempt.vue' }}
    <script setup>
    import { ref, onMounted } from 'vue'
    import { useStore } from '@nanostores/vue'
    import { $clerkStore } from '@clerk/astro/client'

    const clerk = useStore($clerkStore)
    const paymentAttempt = ref(null)

    onMounted(async () => {
      paymentAttempt.value = await clerk.billing.getPaymentAttempt({
        id: 'payment_attempt_123',
      })
    })
    </script>

    <template>
      <pre>{{ JSON.stringify(paymentAttempt.value, null, 2) }}</pre>
    </template>
    ```

    ```svelte {{ filename: 'components/payment-attempt.svelte' }}
    <script>
      // The $ prefix is reserved in Svelte for its own reactivity system.
      // Alias the imports to avoid conflicts.
      import { $clerkStore as clerk } from '@clerk/astro/client'
      import { onMount } from 'svelte'
      let paymentAttempt

      onMount(async () => {
        paymentAttempt = await clerk.billing.getPaymentAttempt({
          id: 'payment_attempt_123',
        })
      })
    </script>

    <pre>{JSON.stringify(paymentAttempt, null, 2)}</pre>
    ```
  </CodeBlockTabs>
</If>

<If sdk={["vue", "nuxt"]}>
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`useClerk()`](/docs/reference/composables/use-clerk) composable to access the `clerk` object, and then call the `getPaymentAttempt()` method.

  ```vue {{ filename: 'payment-attempt.vue' }}
  <script setup>
  import { useClerk } from '@clerk/vue'

  // Use the useClerk composable to access the clerk object
  const clerk = useClerk()

  // Call the getPaymentAttempt() method
  const paymentAttempt = await clerk.billing.getPaymentAttempt({
    id: 'payment_attempt_123',
  })
  </script>

  <template>
    <pre>{{ JSON.stringify(paymentAttempt, null, 2) }}</pre>
  </template>
  ```
</If>

### `getPaymentAttempts()`

Returns a list of payment attempts for the current user or supplied Organization. Returns a [`ClerkPaginatedResponse`](/docs/reference/types/clerk-paginated-response) of [`BillingPaymentResource`](/docs/reference/types/billing-payment-resource) objects.

```ts
function getPaymentAttempts(
  params: GetPaymentAttemptsParams,
): Promise<ClerkPaginatedResponse<BillingPaymentResource>>
```

#### `GetPaymentAttemptsParams`

<Properties>
  - `initialPage?`
  - `number`

  A number that specifies which page to fetch. For example, if `initialPage` is set to `10`, it will skip the first 9 pages and fetch the 10th page.

  ---

  - `pageSize?`
  - `number`

  A number that specifies the maximum number of results to return per page.

  ---

  - `orgId?`
  - `string`

  The Organization ID to perform the request on.
</Properties>

#### Example

<If sdk="js-frontend">
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object.

  ```js {{ filename: 'src/main.js' }}
  import { Clerk } from '@clerk/clerk-js'

  const publishableKey = import.meta.env.VITE_CLERK_PUBLISHABLE_KEY

  // Initialize Clerk with your Clerk Publishable Key
  const clerk = new Clerk(publishableKey)

  // Load Clerk
  await clerk.load()

  // Call the getPaymentAttempts() method
  await clerk.billing.getPaymentAttempts()
  ```
</If>

<If notSdk={["js-frontend", "astro", "vue", "nuxt"]}>
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`useClerk()`](/docs/reference/hooks/use-clerk) hook to access the `clerk.billing` object, and then call the `getPaymentAttempts()` method.

  <If sdk="react">
    ```tsx {{ filename: 'src/pages/Example.tsx' }}
    import { useClerk } from '@clerk/react'

    export default async function Example() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPaymentAttempts() method
      const paymentAttempts = await clerk.billing.getPaymentAttempts()

      return <pre>{JSON.stringify(paymentAttempts, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="nextjs">
    ```tsx {{ filename: 'app/page.tsx' }}
    'use client'

    import { useClerk } from '@clerk/nextjs'

    export default async function Page() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPaymentAttempts() method
      const paymentAttempts = await clerk.billing.getPaymentAttempts()

      return <pre>{JSON.stringify(paymentAttempts, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk={["nextjs", "react"]}>
    You can also use the `usePaymentAttempts()` hook for a simpler way to get the payment attempts.

    <Include src="_partials/hooks/use-payment-attempts" />
  </If>

  <If sdk="react-router">
    ```tsx {{ filename: 'app/routes/home.tsx' }}
    import { useClerk } from '@clerk/react-router'

    export default async function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPaymentAttempts() method
      const paymentAttempts = await clerk.billing.getPaymentAttempts()

      return <pre>{JSON.stringify(paymentAttempts, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="chrome-extension">
    ```tsx {{ filename: 'src/routes/page.tsx' }}
    import { useClerk } from '@clerk/chrome-extension'

    export default async function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPaymentAttempts() method
      const paymentAttempts = await clerk.billing.getPaymentAttempts()

      return <pre>{JSON.stringify(paymentAttempts, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="tanstack-react-start">
    ```tsx {{ filename: 'app/routes/index.tsx' }}
    import { useClerk } from '@clerk/tanstack-react-start'
    import { createFileRoute } from '@tanstack/react-router'

    export const Route = createFileRoute('/')({
      component: Home,
    })

    export default function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPaymentAttempts() method
      const paymentAttempts = await clerk.billing.getPaymentAttempts()

      return <pre>{JSON.stringify(paymentAttempts, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="expo">
    ```tsx {{ filename: 'app/(clerk)/index.tsx' }}
    import { useClerk } from '@clerk/expo'
    import { Text, View } from 'react-native'

    export default async function Page() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPaymentAttempts() method
      const paymentAttempts = await clerk.billing.getPaymentAttempts()

      return (
        <View>
          <Text style={{ whiteSpace: 'pre' }}>{JSON.stringify(paymentAttempts, null, 2)}</Text>
        </View>
      )
    }
    ```
  </If>
</If>

<If sdk="astro">
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`$clerkStore`](/docs/reference/astro/client-side-helpers/clerk-store) to access the `Clerk` object, and then call the `getPaymentAttempts()` method.

  <CodeBlockTabs options={['React', 'Vue', 'Svelte']}>
    ```tsx {{ filename: 'components/payment-attempts.tsx' }}
    import { useStore } from '@nanostores/react'
    import { $clerkStore } from '@clerk/astro/client'

    export default async function Client() {
      // Use the $clerkStore to access the Clerk object
      const clerk = useStore($clerkStore)

      // Call the getPaymentAttempts() method
      const paymentAttempts = await clerk.billing.getPaymentAttempts()

      return <pre>{JSON.stringify(paymentAttempts, null, 2)}</pre>
    }
    ```

    ```vue {{ filename: 'components/payment-attempts.vue' }}
    <script setup>
    import { ref, onMounted } from 'vue'
    import { useStore } from '@nanostores/vue'
    import { $clerkStore } from '@clerk/astro/client'

    const clerk = useStore($clerkStore)
    const paymentAttempts = ref(null)

    // Call the getPaymentAttempts() method
    onMounted(async () => {
      paymentAttempts.value = await clerk.billing.getPaymentAttempts()
    })
    </script>

    <template>
      <pre>{{ JSON.stringify(paymentAttempts.value, null, 2) }}</pre>
    </template>
    ```

    ```svelte {{ filename: 'components/payment-attempts.svelte' }}
    <script>
      // The $ prefix is reserved in Svelte for its own reactivity system.
      // Alias the imports to avoid conflicts.
      import { $clerkStore as clerk } from '@clerk/astro/client'
      import { onMount } from 'svelte'
      let paymentAttempts

      // Call the getPaymentAttempts() method
      onMount(async () => {
        paymentAttempts = await clerk.billing.getPaymentAttempts()
      })
    </script>

    <pre>{JSON.stringify(paymentAttempts, null, 2)}</pre>
    ```
  </CodeBlockTabs>
</If>

<If sdk={["vue", "nuxt"]}>
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`useClerk()`](/docs/reference/composables/use-clerk) composable to access the `clerk` object, and then call the `getPaymentAttempts()` method.

  ```vue {{ filename: 'payment-attempts.vue' }}
  <script setup>
  import { useClerk } from '@clerk/vue'

  // Use the useClerk composable to access the clerk object
  const clerk = useClerk()

  // Call the getPaymentAttempts() method
  const paymentAttempts = await clerk.billing.getPaymentAttempts()
  </script>

  <template>
    <pre>{{ JSON.stringify(paymentAttempts, null, 2) }}</pre>
  </template>
  ```
</If>

### `getPlan()`

Returns a Billing Plan by ID. Returns a [`BillingPlanResource`](/docs/reference/types/billing-plan-resource) object.

```ts
function getPlan(params: GetPlanParams): Promise<BillingPlanResource>
```

#### `GetPlanParams`

<Properties>
  - `id`
  - `string`

  The ID of the Plan to fetch.
</Properties>

#### Example

<If sdk="js-frontend">
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object.

  ```js {{ filename: 'src/main.js' }}
  import { Clerk } from '@clerk/clerk-js'

  const publishableKey = import.meta.env.VITE_CLERK_PUBLISHABLE_KEY

  // Initialize Clerk with your Clerk Publishable Key
  const clerk = new Clerk(publishableKey)

  // Load Clerk
  await clerk.load()

  // Call the getPlan() method
  await clerk.billing.getPlan({
    id: 'plan_123',
  })
  ```
</If>

<If notSdk={["js-frontend", "astro", "vue", "nuxt"]}>
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`useClerk()`](/docs/reference/hooks/use-clerk) hook to access the `clerk.billing` object, and then call the `getPlan()` method.

  <If sdk="react">
    ```tsx {{ filename: 'src/pages/Example.tsx' }}
    import { useClerk } from '@clerk/react'

    export default async function Example() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPlan() method
      const plan = await clerk.billing.getPlan({
        id: 'plan_123',
      })

      return <pre>{JSON.stringify(plan, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="nextjs">
    ```tsx {{ filename: 'app/page.tsx' }}
    'use client'

    import { useClerk } from '@clerk/nextjs'

    export default async function Page() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPlan() method
      const plan = await clerk.billing.getPlan({
        id: 'plan_123',
      })

      return <pre>{JSON.stringify(plan, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="react-router">
    ```tsx {{ filename: 'app/routes/home.tsx' }}
    import { useClerk } from '@clerk/react-router'

    export default async function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPlan() method
      const plan = await clerk.billing.getPlan({
        id: 'plan_123',
      })

      return <pre>{JSON.stringify(plan, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="chrome-extension">
    ```tsx {{ filename: 'src/routes/page.tsx' }}
    import { useClerk } from '@clerk/chrome-extension'

    export default async function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPlan() method
      const plan = await clerk.billing.getPlan({
        id: 'plan_123',
      })

      return <pre>{JSON.stringify(plan, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="tanstack-react-start">
    ```tsx {{ filename: 'app/routes/index.tsx' }}
    import { useClerk } from '@clerk/tanstack-react-start'
    import { createFileRoute } from '@tanstack/react-router'

    export const Route = createFileRoute('/')({
      component: Home,
    })

    export default function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPlan() method
      const plan = await clerk.billing.getPlan({
        id: 'plan_123',
      })

      return <pre>{JSON.stringify(plan, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="expo">
    ```tsx {{ filename: 'app/(clerk)/index.tsx' }}
    import { useClerk } from '@clerk/expo'
    import { Text, View } from 'react-native'

    export default async function Page() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPlan() method
      const plan = await clerk.billing.getPlan({
        id: 'plan_123',
      })

      return (
        <View>
          <Text style={{ whiteSpace: 'pre' }}>{JSON.stringify(plan, null, 2)}</Text>
        </View>
      )
    }
    ```
  </If>
</If>

<If sdk="astro">
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`$clerkStore`](/docs/reference/astro/client-side-helpers/clerk-store) to access the `Clerk` object, and then call the `getPlan()` method.

  <CodeBlockTabs options={['React', 'Vue', 'Svelte']}>
    ```tsx {{ filename: 'components/plan.tsx' }}
    import { useStore } from '@nanostores/react'
    import { $clerkStore } from '@clerk/astro/client'

    export default async function Client() {
      // Use the $clerkStore to access the Clerk object
      const clerk = useStore($clerkStore)

      // Call the getPlan() method
      const plan = await clerk.billing.getPlan({
        id: 'plan_123',
      })

      return <pre>{JSON.stringify(plan, null, 2)}</pre>
    }
    ```

    ```vue {{ filename: 'components/plan.vue' }}
    <script setup>
    import { ref, onMounted } from 'vue'
    import { useStore } from '@nanostores/vue'
    import { $clerkStore } from '@clerk/astro/client'

    const clerk = useStore($clerkStore)
    const plan = ref(null)

    // Call the getPlan() method
    onMounted(async () => {
      plan.value = await clerk.billing.getPlan({
        id: 'plan_123',
      })
    })
    </script>

    <template>
      <pre>{{ JSON.stringify(plan.value, null, 2) }}</pre>
    </template>
    ```

    ```svelte {{ filename: 'components/plan.svelte' }}
    <script>
      // The $ prefix is reserved in Svelte for its own reactivity system.
      // Alias the imports to avoid conflicts.
      import { $clerkStore as clerk } from '@clerk/astro/client'
      import { onMount } from 'svelte'
      let plan

      // Call the getPlan() method
      onMount(async () => {
        plan = await clerk.billing.getPlan({
          id: 'plan_123',
        })
      })
    </script>

    <pre>{JSON.stringify(plan, null, 2)}</pre>
    ```
  </CodeBlockTabs>
</If>

<If sdk={["vue", "nuxt"]}>
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`useClerk()`](/docs/reference/composables/use-clerk) composable to access the `clerk` object, and then call the `getPlan()` method.

  ```vue {{ filename: 'plan.vue' }}
  <script setup>
  import { useClerk } from '@clerk/vue'

  // Use the useClerk composable to access the clerk object
  const clerk = useClerk()

  // Call the getPlan() method
  const plan = await clerk.billing.getPlan({
    id: 'plan_123',
  })
  </script>

  <template>
    <pre>{{ JSON.stringify(plan, null, 2) }}</pre>
  </template>
  ```
</If>

### `getPlans()`

Returns a list of all publicly visible Billing Plans. Returns a [`ClerkPaginatedResponse`](/docs/reference/types/clerk-paginated-response) of [`BillingPlanResource`](/docs/reference/types/billing-plan-resource) objects.

```ts
function getPlans(params?: GetPlansParams): Promise<ClerkPaginatedResponse<BillingPlanResource>>
```

#### `GetPlansParams`

<Properties>
  - `for?`
  - `"user" | "organization"`

  The type of payer for the Plans.

  ---

  - `initialPage?`
  - `number`

  A number that specifies which page to fetch. For example, if `initialPage` is set to `10`, it will skip the first 9 pages and fetch the 10th page.

  ---

  - `pageSize?`
  - `number`

  A number that specifies the maximum number of results to return per page.
</Properties>

#### Example

<If sdk="js-frontend">
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object.

  ```js {{ filename: 'src/main.js' }}
  import { Clerk } from '@clerk/clerk-js'

  const publishableKey = import.meta.env.VITE_CLERK_PUBLISHABLE_KEY

  // Initialize Clerk with your Clerk Publishable Key
  const clerk = new Clerk(publishableKey)

  // Load Clerk
  await clerk.load()

  // Call the getPlans() method
  await clerk.billing.getPlans()
  ```
</If>

<If notSdk={["js-frontend", "astro", "vue", "nuxt"]}>
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`useClerk()`](/docs/reference/hooks/use-clerk) hook to access the `clerk.billing` object, and then call the `getPlans()` method.

  <If sdk="react">
    ```tsx {{ filename: 'src/pages/Example.tsx' }}
    import { useClerk } from '@clerk/react'

    export default async function Example() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPlans() method
      const plans = await clerk.billing.getPlans()

      return <pre>{JSON.stringify(plans, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="nextjs">
    ```tsx {{ filename: 'app/page.tsx' }}
    'use client'

    import { useClerk } from '@clerk/nextjs'

    export default async function Page() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPlans() method
      const plans = await clerk.billing.getPlans()

      return <pre>{JSON.stringify(plans, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk={["nextjs", "react"]}>
    You can also use the [`usePlans()`](/docs/reference/hooks/use-plans) hook for a simpler way to get the plans.

    <Include src="_partials/hooks/use-plans" />
  </If>

  <If sdk="react-router">
    ```tsx {{ filename: 'app/routes/home.tsx' }}
    import { useClerk } from '@clerk/react-router'

    export default async function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPlans() method
      const plans = await clerk.billing.getPlans()

      return <pre>{JSON.stringify(plans, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="chrome-extension">
    ```tsx {{ filename: 'src/routes/page.tsx' }}
    import { useClerk } from '@clerk/chrome-extension'

    export default async function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPlans() method
      const plans = await clerk.billing.getPlans()

      return <pre>{JSON.stringify(plans, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="tanstack-react-start">
    ```tsx {{ filename: 'app/routes/index.tsx' }}
    import { useClerk } from '@clerk/tanstack-react-start'
    import { createFileRoute } from '@tanstack/react-router'

    export const Route = createFileRoute('/')({
      component: Home,
    })

    export default function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPlans() method
      const plans = await clerk.billing.getPlans()

      return <pre>{JSON.stringify(plans, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="expo">
    ```tsx {{ filename: 'app/(clerk)/index.tsx' }}
    import { useClerk } from '@clerk/expo'
    import { Text, View } from 'react-native'

    export default async function Page() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getPlans() method
      const plans = await clerk.billing.getPlans()

      return (
        <View>
          <Text style={{ whiteSpace: 'pre' }}>{JSON.stringify(plans, null, 2)}</Text>
        </View>
      )
    }
    ```
  </If>
</If>

<If sdk="astro">
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`$clerkStore`](/docs/reference/astro/client-side-helpers/clerk-store) to access the `Clerk` object, and then call the `getPlans()` method.

  <CodeBlockTabs options={['React', 'Vue', 'Svelte']}>
    ```tsx {{ filename: 'components/plans.tsx' }}
    import { useStore } from '@nanostores/react'
    import { $clerkStore } from '@clerk/astro/client'

    export default async function Client() {
      // Use the $clerkStore to access the Clerk object
      const clerk = useStore($clerkStore)

      // Call the getPlans() method
      const plans = await clerk.billing.getPlans()

      return <pre>{JSON.stringify(plans, null, 2)}</pre>
    }
    ```

    ```vue {{ filename: 'components/plans.vue' }}
    <script setup>
    import { ref, onMounted } from 'vue'
    import { useStore } from '@nanostores/vue'
    import { $clerkStore } from '@clerk/astro/client'

    const clerk = useStore($clerkStore)
    const plans = ref(null)

    // Call the getPlans() method
    onMounted(async () => {
      plans.value = await clerk.billing.getPlans()
    })
    </script>

    <template>
      <pre>{{ JSON.stringify(plans.value, null, 2) }}</pre>
    </template>
    ```

    ```svelte {{ filename: 'components/plans.svelte' }}
    <script>
      // The $ prefix is reserved in Svelte for its own reactivity system.
      // Alias the imports to avoid conflicts.
      import { $clerkStore as clerk } from '@clerk/astro/client'
      import { onMount } from 'svelte'
      let plans

      // Call the getPlans() method
      onMount(async () => {
        plans = await clerk.billing.getPlans()
      })
    </script>

    <pre>{JSON.stringify(plans, null, 2)}</pre>
    ```
  </CodeBlockTabs>
</If>

<If sdk={["vue", "nuxt"]}>
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`useClerk()`](/docs/reference/composables/use-clerk) composable to access the `clerk` object, and then call the `getPlans()` method.

  ```vue {{ filename: 'plans.vue' }}
  <script setup>
  import { useClerk } from '@clerk/vue'

  // Use the useClerk composable to access the clerk object
  const clerk = useClerk()

  // Call the getPlans() method
  const plans = await clerk.billing.getPlans()
  </script>

  <template>
    <pre>{{ JSON.stringify(plans, null, 2) }}</pre>
  </template>
  ```
</If>

### `getStatement()`

Returns a billing statement by ID. Returns a [`BillingStatementResource`](/docs/reference/types/billing-statement-resource) object.

```ts
function getStatement(params: GetStatementParams): Promise<BillingStatementResource>
```

#### `GetStatementParams`

<Properties>
  - `id`
  - `string`

  The ID of the statement to fetch.

  ---

  - `orgId?`
  - `string`

  The Organization ID to perform the request on.
</Properties>

#### Example

<If sdk="js-frontend">
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object.

  ```js {{ filename: 'src/main.js' }}
  import { Clerk } from '@clerk/clerk-js'

  const publishableKey = import.meta.env.VITE_CLERK_PUBLISHABLE_KEY

  // Initialize Clerk with your Clerk Publishable Key
  const clerk = new Clerk(publishableKey)

  // Load Clerk
  await clerk.load()

  // Call the getStatement() method
  await clerk.billing.getStatement({
    id: 'statement_123',
  })
  ```
</If>

<If notSdk={["js-frontend", "astro", "vue", "nuxt"]}>
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`useClerk()`](/docs/reference/hooks/use-clerk) hook to access the `clerk.billing` object, and then call the `getStatement()` method.

  <If sdk="react">
    ```tsx {{ filename: 'src/pages/Example.tsx' }}
    import { useClerk } from '@clerk/react'

    export default async function Example() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getStatement() method
      const statement = await clerk.billing.getStatement({
        id: 'statement_123',
      })

      return <pre>{JSON.stringify(statement, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="nextjs">
    ```tsx {{ filename: 'app/page.tsx' }}
    'use client'

    import { useClerk } from '@clerk/nextjs'

    export default async function Page() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getStatement() method
      const statement = await clerk.billing.getStatement({
        id: 'statement_123',
      })

      return <pre>{JSON.stringify(statement, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="react-router">
    ```tsx {{ filename: 'app/routes/home.tsx' }}
    import { useClerk } from '@clerk/react-router'

    export default async function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getStatement() method
      const statement = await clerk.billing.getStatement({
        id: 'statement_123',
      })

      return <pre>{JSON.stringify(statement, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="chrome-extension">
    ```tsx {{ filename: 'src/routes/page.tsx' }}
    import { useClerk } from '@clerk/chrome-extension'

    export default async function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getStatement() method
      const statement = await clerk.billing.getStatement({
        id: 'statement_123',
      })

      return <pre>{JSON.stringify(statement, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="tanstack-react-start">
    ```tsx {{ filename: 'app/routes/index.tsx' }}
    import { useClerk } from '@clerk/tanstack-react-start'
    import { createFileRoute } from '@tanstack/react-router'

    export const Route = createFileRoute('/')({
      component: Home,
    })

    export default function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getStatement() method
      const statement = await clerk.billing.getStatement({
        id: 'statement_123',
      })

      return <pre>{JSON.stringify(statement, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="expo">
    ```tsx {{ filename: 'app/(clerk)/index.tsx' }}
    import { useClerk } from '@clerk/expo'
    import { Text, View } from 'react-native'

    export default async function Page() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getStatement() method
      const statement = await clerk.billing.getStatement({
        id: 'statement_123',
      })

      return (
        <View>
          <Text style={{ whiteSpace: 'pre' }}>{JSON.stringify(statement, null, 2)}</Text>
        </View>
      )
    }
    ```
  </If>
</If>

<If sdk="astro">
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`$clerkStore`](/docs/reference/astro/client-side-helpers/clerk-store) to access the `Clerk` object, and then call the `getStatement()` method.

  <CodeBlockTabs options={['React', 'Vue', 'Svelte']}>
    ```tsx {{ filename: 'components/statement.tsx' }}
    import { useStore } from '@nanostores/react'
    import { $clerkStore } from '@clerk/astro/client'

    export default async function Client() {
      // Use the $clerkStore to access the Clerk object
      const clerk = useStore($clerkStore)

      // Call the getStatement() method
      const statement = await clerk.billing.getStatement({
        id: 'statement_123',
      })

      return <pre>{JSON.stringify(statement, null, 2)}</pre>
    }
    ```

    ```vue {{ filename: 'components/statement.vue' }}
    <script setup>
    import { ref, onMounted } from 'vue'
    import { useStore } from '@nanostores/vue'
    import { $clerkStore } from '@clerk/astro/client'

    const clerk = useStore($clerkStore)
    const statement = ref(null)

    // Call the getStatement() method
    onMounted(async () => {
      statement.value = await clerk.billing.getStatement({
        id: 'statement_123',
      })
    })
    </script>

    <template>
      <pre>{{ JSON.stringify(statement.value, null, 2) }}</pre>
    </template>
    ```

    ```svelte {{ filename: 'components/statement.svelte' }}
    <script>
      // The $ prefix is reserved in Svelte for its own reactivity system.
      // Alias the imports to avoid conflicts.
      import { $clerkStore as clerk } from '@clerk/astro/client'
      import { onMount } from 'svelte'
      let statement

      // Call the getStatement() method
      onMount(async () => {
        statement = await clerk.billing.getStatement({
          id: 'statement_123',
        })
      })
    </script>

    <pre>{JSON.stringify(statement, null, 2)}</pre>
    ```
  </CodeBlockTabs>
</If>

<If sdk={["vue", "nuxt"]}>
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`useClerk()`](/docs/reference/composables/use-clerk) composable to access the `clerk` object, and then call the `getStatement()` method.

  ```vue {{ filename: 'statement.vue' }}
  <script setup>
  import { useClerk } from '@clerk/vue'

  // Use the useClerk composable to access the clerk object
  const clerk = useClerk()

  // Call the getStatement() method
  const statement = await clerk.billing.getStatement({
    id: 'statement_123',
  })
  </script>

  <template>
    <pre>{{ JSON.stringify(statement, null, 2) }}</pre>
  </template>
  ```
</If>

### `getStatements()`

Returns a list of billing statements for the current user or supplied Organization. Returns a [`ClerkPaginatedResponse`](/docs/reference/types/clerk-paginated-response) of [`BillingStatementResource`](/docs/reference/types/billing-statement-resource) objects.

```ts
function getStatements(
  params: GetStatementsParams,
): Promise<ClerkPaginatedResponse<BillingStatementResource>>
```

#### `GetStatementsParams`

<Properties>
  - `initialPage?`
  - `number`

  A number that specifies which page to fetch. For example, if `initialPage` is set to `10`, it will skip the first 9 pages and fetch the 10th page.

  ---

  - `pageSize?`
  - `number`

  A number that specifies the maximum number of results to return per page.

  ---

  - `orgId?`
  - `string`

  The Organization ID to perform the request on.
</Properties>

#### Example

<If sdk="js-frontend">
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object.

  ```js {{ filename: 'src/main.js' }}
  import { Clerk } from '@clerk/clerk-js'

  const publishableKey = import.meta.env.VITE_CLERK_PUBLISHABLE_KEY

  // Initialize Clerk with your Clerk Publishable Key
  const clerk = new Clerk(publishableKey)

  // Load Clerk
  await clerk.load()

  // Call the getStatements() method
  await clerk.billing.getStatements()
  ```
</If>

<If notSdk={["js-frontend", "astro", "vue", "nuxt"]}>
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`useClerk()`](/docs/reference/hooks/use-clerk) hook to access the `clerk.billing` object, and then call the `getStatements()` method.

  <If sdk="react">
    ```tsx {{ filename: 'src/pages/Example.tsx' }}
    import { useClerk } from '@clerk/react'

    export default async function Example() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getStatements() method
      const statements = await clerk.billing.getStatements()

      return <pre>{JSON.stringify(statements, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="nextjs">
    ```tsx {{ filename: 'app/page.tsx' }}
    'use client'

    import { useClerk } from '@clerk/nextjs'

    export default async function Page() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getStatements() method
      const statements = await clerk.billing.getStatements()

      return <pre>{JSON.stringify(statements, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk={["nextjs", "react"]}>
    You can also use the [`useStatements()`](/docs/reference/hooks/use-statements) hook for a simpler way to get the statements.

    <Include src="_partials/hooks/use-statements" />
  </If>

  <If sdk="react-router">
    ```tsx {{ filename: 'app/routes/home.tsx' }}
    import { useClerk } from '@clerk/react-router'

    export default async function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getStatements() method
      const statements = await clerk.billing.getStatements()

      return <pre>{JSON.stringify(statements, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="chrome-extension">
    ```tsx {{ filename: 'src/routes/page.tsx' }}
    import { useClerk } from '@clerk/chrome-extension'

    export default async function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getStatements() method
      const statements = await clerk.billing.getStatements()

      return <pre>{JSON.stringify(statements, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="tanstack-react-start">
    ```tsx {{ filename: 'app/routes/index.tsx' }}
    import { useClerk } from '@clerk/tanstack-react-start'
    import { createFileRoute } from '@tanstack/react-router'

    export const Route = createFileRoute('/')({
      component: Home,
    })

    export default function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getStatements() method
      const statements = await clerk.billing.getStatements()

      return <pre>{JSON.stringify(statements, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="expo">
    ```tsx {{ filename: 'app/(clerk)/index.tsx' }}
    import { useClerk } from '@clerk/expo'
    import { Text, View } from 'react-native'

    export default async function Page() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getStatements() method
      const statements = await clerk.billing.getStatements()

      return (
        <View>
          <Text style={{ whiteSpace: 'pre' }}>{JSON.stringify(statements, null, 2)}</Text>
        </View>
      )
    }
    ```
  </If>
</If>

<If sdk="astro">
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`$clerkStore`](/docs/reference/astro/client-side-helpers/clerk-store) to access the `Clerk` object, and then call the `getStatements()` method.

  <CodeBlockTabs options={['React', 'Vue', 'Svelte']}>
    ```tsx {{ filename: 'components/statements.tsx' }}
    import { useStore } from '@nanostores/react'
    import { $clerkStore } from '@clerk/astro/client'

    export default async function Client() {
      // Use the $clerkStore to access the Clerk object
      const clerk = useStore($clerkStore)

      // Call the getStatements() method
      const statements = await clerk.billing.getStatements()

      return <pre>{JSON.stringify(statements, null, 2)}</pre>
    }
    ```

    ```vue {{ filename: 'components/statements.vue' }}
    <script setup>
    import { ref, onMounted } from 'vue'
    import { useStore } from '@nanostores/vue'
    import { $clerkStore } from '@clerk/astro/client'

    const clerk = useStore($clerkStore)
    const statements = ref(null)

    // Call the getStatements() method
    onMounted(async () => {
      statements.value = await clerk.billing.getStatements()
    })
    </script>

    <template>
      <pre>{{ JSON.stringify(statements.value, null, 2) }}</pre>
    </template>
    ```

    ```svelte {{ filename: 'components/statements.svelte' }}
    <script>
      // The $ prefix is reserved in Svelte for its own reactivity system.
      // Alias the imports to avoid conflicts.
      import { $clerkStore as clerk } from '@clerk/astro/client'
      import { onMount } from 'svelte'
      let statements

      // Call the getStatements() method
      onMount(async () => {
        statements = await clerk.billing.getStatements()
      })
    </script>

    <pre>{JSON.stringify(statements, null, 2)}</pre>
    ```
  </CodeBlockTabs>
</If>

<If sdk={["vue", "nuxt"]}>
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`useClerk()`](/docs/reference/composables/use-clerk) composable to access the `clerk` object, and then call the `getStatements()` method.

  ```vue {{ filename: 'statements.vue' }}
  <script setup>
  import { useClerk } from '@clerk/vue'

  // Use the useClerk composable to access the clerk object
  const clerk = useClerk()

  // Call the getStatements() method
  const statements = await clerk.billing.getStatements()
  </script>

  <template>
    <pre>{{ JSON.stringify(statements, null, 2) }}</pre>
  </template>
  ```
</If>

### `getSubscription()`

Returns the main Billing Subscription for the current user or supplied Organization. Returns a [`BillingSubscriptionResource`](/docs/reference/types/billing-subscription-resource) object.

```ts
function getSubscription(params: GetSubscriptionParams): Promise<BillingSubscriptionResource>
```

#### `GetSubscriptionParams`

<Properties>
  - `orgId?`
  - `string`

  The Organization ID to perform the request on.
</Properties>

#### Example

<If sdk="js-frontend">
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object.

  ```js {{ filename: 'src/main.js' }}
  import { Clerk } from '@clerk/clerk-js'

  const publishableKey = import.meta.env.VITE_CLERK_PUBLISHABLE_KEY

  // Initialize Clerk with your Clerk Publishable Key
  const clerk = new Clerk(publishableKey)

  // Load Clerk
  await clerk.load()

  // Call the getSubscription() method
  await clerk.billing.getSubscription({
    orgId: 'org_123',
  })
  ```
</If>

<If notSdk={["js-frontend", "astro", "vue", "nuxt"]}>
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`useClerk()`](/docs/reference/hooks/use-clerk) hook to access the `clerk.billing` object, and then call the `getSubscription()` method.

  <If sdk={["nextjs", "react"]}>
    > [!IMPORTANT]
    > You can also use the `useSubscription()` hook for a simpler way to manage subscriptions. See the [reference documentation](/docs/reference/hooks/use-subscription) for more information.
  </If>

  <If sdk="react">
    ```tsx {{ filename: 'src/pages/Example.tsx' }}
    import { useClerk } from '@clerk/react'

    export default async function Example() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getSubscription() method
      const subscription = await clerk.billing.getSubscription({
        orgId: 'org_123',
      })

      return <pre>{JSON.stringify(subscription, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="nextjs">
    ```tsx {{ filename: 'app/page.tsx' }}
    'use client'

    import { useClerk } from '@clerk/nextjs'

    export default async function Page() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getSubscription() method
      const subscription = await clerk.billing.getSubscription({
        orgId: 'org_123',
      })

      return <pre>{JSON.stringify(subscription, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="react-router">
    ```tsx {{ filename: 'app/routes/home.tsx' }}
    import { useClerk } from '@clerk/react-router'

    export default async function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getSubscription() method
      const subscription = await clerk.billing.getSubscription({
        orgId: 'org_123',
      })

      return <pre>{JSON.stringify(subscription, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="chrome-extension">
    ```tsx {{ filename: 'src/routes/page.tsx' }}
    import { useClerk } from '@clerk/chrome-extension'

    export default async function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getSubscription() method
      const subscription = await clerk.billing.getSubscription({
        orgId: 'org_123',
      })

      return <pre>{JSON.stringify(subscription, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="tanstack-react-start">
    ```tsx {{ filename: 'app/routes/index.tsx' }}
    import { useClerk } from '@clerk/tanstack-react-start'
    import { createFileRoute } from '@tanstack/react-router'

    export const Route = createFileRoute('/')({
      component: Home,
    })

    export default function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getSubscription() method
      const subscription = await clerk.billing.getSubscription({
        orgId: 'org_123',
      })

      return <pre>{JSON.stringify(subscription, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="expo">
    ```tsx {{ filename: 'app/(clerk)/index.tsx' }}
    import { useClerk } from '@clerk/expo'
    import { Text, View } from 'react-native'

    export default async function Page() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the getSubscription() method
      const subscription = await clerk.billing.getSubscription({
        orgId: 'org_123',
      })

      return (
        <View>
          <Text style={{ whiteSpace: 'pre' }}>{JSON.stringify(subscription, null, 2)}</Text>
        </View>
      )
    }
    ```
  </If>
</If>

<If sdk="astro">
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`$clerkStore`](/docs/reference/astro/client-side-helpers/clerk-store) to access the `Clerk` object, and then call the `getSubscription()` method.

  <CodeBlockTabs options={['React', 'Vue', 'Svelte']}>
    ```tsx {{ filename: 'components/subscription.tsx' }}
    import { useStore } from '@nanostores/react'
    import { $clerkStore } from '@clerk/astro/client'

    export default async function Client() {
      // Use the $clerkStore to access the Clerk object
      const clerk = useStore($clerkStore)

      // Call the getSubscription() method
      const subscription = await clerk.billing.getSubscription({
        orgId: 'org_123',
      })

      return <pre>{JSON.stringify(subscription, null, 2)}</pre>
    }
    ```

    ```vue {{ filename: 'components/subscription.vue' }}
    <script setup>
    import { ref, onMounted } from 'vue'
    import { useStore } from '@nanostores/vue'
    import { $clerkStore } from '@clerk/astro/client'

    const clerk = useStore($clerkStore)
    const subscription = ref(null)

    // Call the getSubscription() method
    onMounted(async () => {
      subscription.value = await clerk.billing.getSubscription({
        orgId: 'org_123',
      })
    })
    </script>

    <template>
      <pre>{{ JSON.stringify(subscription.value, null, 2) }}</pre>
    </template>
    ```

    ```svelte {{ filename: 'components/subscription.svelte' }}
    <script>
      // The $ prefix is reserved in Svelte for its own reactivity system.
      // Alias the imports to avoid conflicts.
      import { $clerkStore as clerk } from '@clerk/astro/client'
      import { onMount } from 'svelte'
      let subscription

      // Call the getSubscription() method
      onMount(async () => {
        subscription = await clerk.billing.getSubscription({
          orgId: 'org_123',
        })
      })
    </script>

    <pre>{JSON.stringify(subscription, null, 2)}</pre>
    ```
  </CodeBlockTabs>
</If>

<If sdk={["vue", "nuxt"]}>
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`useClerk()`](/docs/reference/composables/use-clerk) composable to access the `clerk` object, and then call the `getSubscription()` method.

  ```vue {{ filename: 'subscription.vue' }}
  <script setup>
  import { useClerk } from '@clerk/vue'

  // Use the useClerk composable to access the clerk object
  const clerk = useClerk()

  // Call the getSubscription() method
  const subscription = await clerk.billing.getSubscription({
    orgId: 'org_123',
  })
  </script>

  <template>
    <pre>{{ JSON.stringify(subscription, null, 2) }}</pre>
  </template>
  ```
</If>

### `startCheckout()`

Creates a new billing checkout for the current user or supplied Organization. Returns a [`BillingCheckoutResource`](/docs/reference/types/billing-checkout-resource) object.

```ts
function startCheckout(params: CreateCheckoutParams): Promise<BillingCheckoutResource>
```

#### `CreateCheckoutParams`

<Properties>
  - `planId`
  - `string`

  The unique identifier for the Plan.

  ---

  - `planPeriod`
  - `"month" | "annual"`

  The billing period for the Plan.

  ---

  - `orgId?`
  - `string`

  The Organization ID to perform the request on.
</Properties>

#### Example

<If sdk="js-frontend">
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object.

  ```js {{ filename: 'src/main.js' }}
  import { Clerk } from '@clerk/clerk-js'

  const publishableKey = import.meta.env.VITE_CLERK_PUBLISHABLE_KEY

  // Initialize Clerk with your Clerk Publishable Key
  const clerk = new Clerk(publishableKey)

  // Load Clerk
  await clerk.load()

  // Call the startCheckout() method
  await clerk.billing.startCheckout({
    planId: 'plan_123',
    planPeriod: 'month',
  })
  ```
</If>

<If notSdk={["js-frontend", "astro", "vue", "nuxt"]}>
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`useClerk()`](/docs/reference/hooks/use-clerk) hook to access the `clerk.billing` object, and then call the `startCheckout()` method.

  <If sdk={["nextjs", "react"]}>
    > [!IMPORTANT]
    > You can also use the `useCheckout()` hook for a simpler way to start a checkout. See the [reference documentation](/docs/reference/hooks/use-checkout) for more information.
  </If>

  <If sdk="react">
    ```tsx {{ filename: 'src/pages/Example.tsx' }}
    import { useClerk } from '@clerk/react'

    export default async function Example() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the startCheckout() method
      const checkout = await clerk.billing.startCheckout({
        planId: 'plan_123',
        planPeriod: 'month',
      })

      return <pre>{JSON.stringify(checkout, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="nextjs">
    ```tsx {{ filename: 'app/page.tsx' }}
    'use client'

    import { useClerk } from '@clerk/nextjs'

    export default async function Page() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the startCheckout() method
      const checkout = await clerk.billing.startCheckout({
        planId: 'plan_123',
        planPeriod: 'month',
      })

      return <pre>{JSON.stringify(checkout, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="react-router">
    ```tsx {{ filename: 'app/routes/home.tsx' }}
    import { useClerk } from '@clerk/react-router'

    export default async function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the startCheckout() method
      const checkout = await clerk.billing.startCheckout({
        planId: 'plan_123',
        planPeriod: 'month',
      })

      return <pre>{JSON.stringify(checkout, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="chrome-extension">
    ```tsx {{ filename: 'src/routes/page.tsx' }}
    import { useClerk } from '@clerk/chrome-extension'

    export default async function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the startCheckout() method
      const checkout = await clerk.billing.startCheckout({
        planId: 'plan_123',
        planPeriod: 'month',
      })

      return <pre>{JSON.stringify(checkout, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="tanstack-react-start">
    ```tsx {{ filename: 'app/routes/index.tsx' }}
    import { useClerk } from '@clerk/tanstack-react-start'
    import { createFileRoute } from '@tanstack/react-router'

    export const Route = createFileRoute('/')({
      component: Home,
    })

    export default function Home() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the startCheckout() method
      const checkout = await clerk.billing.startCheckout({
        planId: 'plan_123',
        planPeriod: 'month',
      })

      return <pre>{JSON.stringify(checkout, null, 2)}</pre>
    }
    ```
  </If>

  <If sdk="expo">
    ```tsx {{ filename: 'app/(clerk)/index.tsx' }}
    import { useClerk } from '@clerk/expo'
    import { Text, View } from 'react-native'

    export default async function Page() {
      // Use the useClerk hook to access the clerk object
      const clerk = useClerk()

      // Call the startCheckout() method
      const checkout = await clerk.billing.startCheckout({
        planId: 'plan_123',
        planPeriod: 'month',
      })

      return (
        <View>
          <Text style={{ whiteSpace: 'pre' }}>{JSON.stringify(checkout, null, 2)}</Text>
        </View>
      )
    }
    ```
  </If>
</If>

<If sdk="astro">
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`$clerkStore`](/docs/reference/astro/client-side-helpers/clerk-store) to access the `Clerk` object, and then call the `startCheckout()` method.

  <CodeBlockTabs options={['React', 'Vue', 'Svelte']}>
    ```tsx {{ filename: 'components/checkout.tsx' }}
    import { useStore } from '@nanostores/react'
    import { $clerkStore } from '@clerk/astro/client'

    export default async function Client() {
      // Use the $clerkStore to access the Clerk object
      const clerk = useStore($clerkStore)

      // Call the startCheckout() method
      const checkout = await clerk.billing.startCheckout({
        planId: 'plan_123',
        planPeriod: 'month',
      })

      return <pre>{JSON.stringify(checkout, null, 2)}</pre>
    }
    ```

    ```vue {{ filename: 'components/checkout.vue' }}
    <script setup>
    import { ref, onMounted } from 'vue'
    import { useStore } from '@nanostores/vue'
    import { $clerkStore } from '@clerk/astro/client'

    const clerk = useStore($clerkStore)
    const checkout = ref(null)

    // Call the startCheckout() method
    onMounted(async () => {
      checkout.value = await clerk.billing.startCheckout({
        planId: 'plan_123',
        planPeriod: 'month',
      })
    })
    </script>

    <template>
      <pre>{{ JSON.stringify(checkout.value, null, 2) }}</pre>
    </template>
    ```

    ```svelte {{ filename: 'components/checkout.svelte' }}
    <script>
      // The $ prefix is reserved in Svelte for its own reactivity system.
      // Alias the imports to avoid conflicts.
      import { $clerkStore as clerk } from '@clerk/astro/client'
      import { onMount } from 'svelte'
      let checkout

      // Call the startCheckout() method
      onMount(async () => {
        checkout = await clerk.billing.startCheckout({
          planId: 'plan_123',
          planPeriod: 'month',
        })
      })
    </script>

    <pre>{JSON.stringify(checkout, null, 2)}</pre>
    ```
  </CodeBlockTabs>
</If>

<If sdk={["vue", "nuxt"]}>
  The `Billing` object is available on the [`Clerk`](/docs/reference/objects/clerk) object. Use the [`useClerk()`](/docs/reference/composables/use-clerk) composable to access the `clerk` object, and then call the `startCheckout()` method.

  ```vue {{ filename: 'checkout.vue' }}
  <script setup>
  import { useClerk } from '@clerk/vue'

  // Use the useClerk composable to access the clerk object
  const clerk = useClerk()

  // Call the startCheckout() method
  const checkout = await clerk.billing.startCheckout({
    planId: 'plan_123',
    planPeriod: 'month',
  })
  </script>

  <template>
    <pre>{{ JSON.stringify(checkout, null, 2) }}</pre>
  </template>
  ```
</If>
