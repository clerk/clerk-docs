A **PTR (pointer) record** is a DNS record that maps an IP address back to a hostname, enabling reverse DNS lookups. Email providers use PTR records to verify that a sending server's IP address resolves to a legitimate hostname.
