---
title: React Authentication
description: Complete AI prompt for integrating Clerk authentication into React applications
---

# React Authentication with Clerk

This prompt helps AI assistants guide you through implementing Clerk authentication in React applications, including SPAs and client-side rendered apps.

## When to use this prompt

- Building a new React application with Clerk
- Adding authentication to an existing React app
- Implementing custom authentication flows
- Building organization and team features

## The prompt

```text copyButton
You are an expert in React and Clerk authentication. Help me implement Clerk in my React application following these guidelines:

## Core Principles
1. Use React 18+ features and patterns
2. Implement proper component composition
3. Handle authentication state correctly
4. Follow React best practices and hooks rules

## Setup Steps
1. Install required packages: @clerk/clerk-react
2. Configure environment variables: VITE_CLERK_PUBLISHABLE_KEY (for Vite) or REACT_APP_CLERK_PUBLISHABLE_KEY (for CRA)
3. Wrap the app with ClerkProvider
4. Set up routing with authentication guards

## Key Patterns
- Use useAuth() hook for authentication state
- Use useUser() hook for user data
- Implement protected routes with custom components
- Handle loading and error states properly
- Manage authentication flows with Clerk components

## Component Architecture
- Create reusable authentication guards
- Build custom sign-in/sign-up flows when needed
- Implement proper error boundaries
- Use Suspense for loading states

## Common Tasks
- Setting up ClerkProvider with routing
- Creating protected routes
- Building custom user profiles
- Implementing organization switchers
- Managing user sessions

When providing code examples, always:
- Use functional components with hooks
- Include proper TypeScript types
- Show loading and error states
- Follow React naming conventions

[Add your specific React prompt content here]
```

## Usage examples

### Basic setup

```typescript
// main.tsx or index.tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import { ClerkProvider } from '@clerk/clerk-react'
import App from './App'

const clerkPubKey = import.meta.env.VITE_CLERK_PUBLISHABLE_KEY

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ClerkProvider publishableKey={clerkPubKey} afterSignOutUrl="/">
      <App />
    </ClerkProvider>
  </React.StrictMode>,
)
```

### Protected routes

```typescript
// components/ProtectedRoute.tsx
import { useAuth } from '@clerk/clerk-react'
import { Navigate } from 'react-router-dom'

export function ProtectedRoute({ children }: { children: React.ReactNode }) {
  const { isLoaded, isSignedIn } = useAuth()
  
  if (!isLoaded) {
    return <div>Loading...</div>
  }
  
  if (!isSignedIn) {
    return <Navigate to="/sign-in" replace />
  }
  
  return <>{children}</>
}
```

### Using user data

```typescript
// components/UserProfile.tsx
import { useUser } from '@clerk/clerk-react'

export function UserProfile() {
  const { user, isLoaded } = useUser()
  
  if (!isLoaded) {
    return <div>Loading user...</div>
  }
  
  if (!user) {
    return null
  }
  
  return (
    <div>
      <img src={user.imageUrl} alt={user.firstName || 'User'} />
      <h2>{user.fullName}</h2>
      <p>{user.primaryEmailAddress?.emailAddress}</p>
    </div>
  )
}
```

## Tips for AI tools

- Ensure all Clerk hooks are used within ClerkProvider
- Always handle loading states when using Clerk hooks
- Verify that environment variables are properly prefixed
- Check that TypeScript types are imported from `@clerk/clerk-react`

## Related prompts

- [Next.js Applications](/docs/ai-prompts/nextjs)
- [Supabase Integration](/docs/ai-prompts/supabase)