**`Experimental`**

This is an experimental API for the Billing feature that is available under a public beta, and the API is subject to change.
It is advised to pin the SDK version and the clerk-js version to a specific version to avoid breaking changes.

## Example

```tsx
<ClerkProvider clerkJsVersion="x.x.x" />
```

## Properties

| Property                                          | Type                        | Description                                                                                                                                                                                                                                                                                                         | Overrides                                                           |
| ------------------------------------------------- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| <a id="createdat"></a> `createdAt`                | `Date`                      | **`Experimental`** This is an experimental API for the Billing feature that is available under a public beta, and the API is subject to change. It is advised to pin the SDK version and the clerk-js version to a specific version to avoid breaking changes. Example: `<ClerkProvider clerkJsVersion="x.x.x" />`. | -                                                                   |
| <a id="email"></a> `email?`                       | `string`                    | **`Experimental`** This is an experimental API for the Billing feature that is available under a public beta, and the API is subject to change. It is advised to pin the SDK version and the clerk-js version to a specific version to avoid breaking changes. Example: `<ClerkProvider clerkJsVersion="x.x.x" />`. | -                                                                   |
| <a id="firstname"></a> `firstName?`               | `string`                    | **`Experimental`** This is an experimental API for the Billing feature that is available under a public beta, and the API is subject to change. It is advised to pin the SDK version and the clerk-js version to a specific version to avoid breaking changes. Example: `<ClerkProvider clerkJsVersion="x.x.x" />`. | -                                                                   |
| <a id="id"></a> `id`                              | `string`                    | **`Experimental`** This is an experimental API for the Billing feature that is available under a public beta, and the API is subject to change. It is advised to pin the SDK version and the clerk-js version to a specific version to avoid breaking changes. Example: `<ClerkProvider clerkJsVersion="x.x.x" />`. | [`ClerkResource`](clerk-resource.mdx).[`id`](clerk-resource.mdx#id) |
| <a id="imageurl"></a> `imageUrl`                  | <code>null \| string</code> | **`Experimental`** This is an experimental API for the Billing feature that is available under a public beta, and the API is subject to change. It is advised to pin the SDK version and the clerk-js version to a specific version to avoid breaking changes. Example: `<ClerkProvider clerkJsVersion="x.x.x" />`. | -                                                                   |
| <a id="lastname"></a> `lastName?`                 | `string`                    | **`Experimental`** This is an experimental API for the Billing feature that is available under a public beta, and the API is subject to change. It is advised to pin the SDK version and the clerk-js version to a specific version to avoid breaking changes. Example: `<ClerkProvider clerkJsVersion="x.x.x" />`. | -                                                                   |
| <a id="organizationid"></a> `organizationId?`     | `string`                    | **`Experimental`** This is an experimental API for the Billing feature that is available under a public beta, and the API is subject to change. It is advised to pin the SDK version and the clerk-js version to a specific version to avoid breaking changes. Example: `<ClerkProvider clerkJsVersion="x.x.x" />`. | -                                                                   |
| <a id="organizationname"></a> `organizationName?` | `string`                    | **`Experimental`** This is an experimental API for the Billing feature that is available under a public beta, and the API is subject to change. It is advised to pin the SDK version and the clerk-js version to a specific version to avoid breaking changes. Example: `<ClerkProvider clerkJsVersion="x.x.x" />`. | -                                                                   |
| <a id="pathroot"></a> `pathRoot`                  | `string`                    | The root path of the resource.                                                                                                                                                                                                                                                                                      | -                                                                   |
| <a id="updatedat"></a> `updatedAt`                | `Date`                      | **`Experimental`** This is an experimental API for the Billing feature that is available under a public beta, and the API is subject to change. It is advised to pin the SDK version and the clerk-js version to a specific version to avoid breaking changes. Example: `<ClerkProvider clerkJsVersion="x.x.x" />`. | -                                                                   |
| <a id="userid"></a> `userId?`                     | `string`                    | **`Experimental`** This is an experimental API for the Billing feature that is available under a public beta, and the API is subject to change. It is advised to pin the SDK version and the clerk-js version to a specific version to avoid breaking changes. Example: `<ClerkProvider clerkJsVersion="x.x.x" />`. | -                                                                   |

## Methods

### reload()

Reload the resource and return the resource itself.

#### Parameters

| Parameter | Type                        |
| --------- | --------------------------- |
| `p?`      | `ClerkResourceReloadParams` |

#### Returns

`Promise`\<`CommercePayerResource`\>

#### Inherited from

[`ClerkResource`](clerk-resource.mdx).[`reload`](clerk-resource.mdx#reload)
