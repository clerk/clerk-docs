---
title: '`PasskeyResource`'
description: An interface that describes a passkey associated with a user response.
sdk: js-frontend, astro, chrome-extension, expo, nextjs, react, react-router, tanstack-react-start, nuxt, vue
---

An interface that describes a passkey associated with a user response.

## Properties

<Properties>
  - `createdAt`
  - `Date`

  The date when the passkey was created.

  ---

  - `id`
  - `string`

  The unique identifier of the passkey.

  ---

  - `lastUsedAt`
  - `Date | null`

  The date when the passkey was last used.

  ---

  - `name`
  - `string`

  The passkey's name.

  ---

  - `updatedAt`
  - `Date`

  The date when the passkey was last updated.

  ---

  - `verification`
  - [`VerificationResource`](/docs/reference/types/verification-resource)

  The verification details for the passkey.
</Properties>

## Methods

### `update()`

Updates the name of the associated passkey for the signed-in user.

```ts
function update(params: { name: string }): Promise<PasskeyResource>
```

For an example of how to use these methods, see the [Passkeys custom flows documentation](/docs/guides/development/custom-flows/authentication/passkeys#rename-user-passkeys).

### `delete()`

Deletes the associated passkey for the signed-in user.

```ts {{ prettier: false }}
function delete(): Promise<DeletedObjectResource>
```

Learn more:

- [`DeletedObjectResource`](/docs/reference/types/deleted-object-resource)

For an example of how to use these methods, see the [Passkeys custom flows documentation](/docs/guides/development/custom-flows/authentication/passkeys#delete-user-passkeys).
