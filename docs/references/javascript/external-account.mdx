---
title: ExternalAccount
description: The ExternalAccount object is a model around an identification obtained by an external provider (e.g. an OAuth provider such as Google).
---

import { Tab, Tabs } from "nextra-theme-docs";
import { Tables } from "@/components/Table";
import { Images } from "@/components/Images";
import { CodeBlockTabs } from "@/components/CodeBlockTabs";

# `ExternalAccount`

The `ExternalAccount` object is a model around an identification obtained by an external provider (e.g. an OAuth provider such as Google).

External account must be verified, so that you can make sure they can be assigned to their rightful owners. The `ExternalAccount` object holds all necessary state around the verification process.

## Attributes


<Tables
  headings={["Name", "Type", "Description"]}
  headingsMeta={[{maxWidth:'300px'},{maxWidth: '300px'},{maxWidth: '300px'}]}
  rows={[
 {cells: [<code>id</code>, <code>string</code>, <>A unique identifier for this external account.</>]},
{cells: [<code>identificationId</code>, <code>string</code>, <>The identification with which this external account is associated.</>]},
{cells: [<code>provider</code>, <code>string</code>, <>The provider name e.g. `google`</>]},
{cells: [<code>publicMetadata</code>, <code>object</code>, <>Metadata provided about the user from the provider</>]},
{cells: [<code>providerUserId</code>, <code>string</code>, <>The unique id of the user in the provider.</>]},
{cells: [<code>emailAddress</code>, <code>string</code>, <>The provided email address of the user.</>]},
{cells: [<code>approvedScopes</code>, <code>string[]</code>, <>The scopes that the user has granted access to.</>]},
{cells: [<code>firstName</code>, <code>string</code>, <>The provided first name of the user.</>]},
{cells: [<code>lastName</code>, <code>string</code>, <>The provided last name of the user.</>]},
{cells: [<code>avatarUrl</code>, <code>string</code>, <>The provided avatar URL of the user.</>]},
{cells: [<code>username</code>, <code>string | null</code>, <>The provided username of the user.</>]},
{cells: [<code>label</code>, <code>string | null</code>, <>A descriptive label to differentiate multiple external accounts of the same user for the same provider.</>]},
{cells: [<code>verification</code>, <a href="/docs/references/javascript/verification"><code>Verification</code></a>, <>An object holding information on the verification of this external account.</>]}
   ]}
/>

## Methods

### `reauthorize()`
```typescript
function reauthorize(params: ReauthorizeExternalAccountParams): Promise<ExternalAccount>;
```

Invoke a re-authorization flow for an existing external account.

#### `ReauthorizeExternalAccountParams`

<Tables
  headings={["Name", "Type", "Description"]}
  headingsMeta={[{maxWidth:'300px'},{maxWidth: '300px'},{maxWidth: '300px'}]}
  rows={[
{cells: [<code>additionalScopes</code>, <code>string[] | undefined</code>, <>Any additional scopes you would like your user to be prompted to approve.</>]},
{cells: [<code>redirectUrl</code>, <code>string | undefined</code>, <>The URL to redirect back to one the OAuth flow has completed successfully or unsuccessfully.</>]},
  ]}
/>

### `destroy()`
```typescript
function destroy(): Promise<void>;
```

Delete this external account.


### `providerSlug()`

```typescript
function providerSlug(): string;
```

A getter method for the `provider` attribute

### `providerTitle()`

```typescript
function providerTitle(): string;
```

The title of the provider with the word "Account" appended.

EG: `google` becomes `Google Account`

### `accountIdentifier()`

```typescript
function accountIdentifier(): string;
```

The identifier of the account, either the:

- `username` if present
- `emailAddress` if present
- `label`