---
title: '`$sessionStore`'
description: Clerk's $sessionStore nanostore provides a convenient way to access the current user Session object, as well as helpers to set the session.
sdk: astro
---

The `$sessionStore` store provides a convenient way to access the current user's [`Session`](/docs/reference/objects/session){{ target: '_blank' }} object, as well as helpers for setting the active session.

## How to use the `$sessionStore` store

<Include src="_partials/astro/session-store" />
