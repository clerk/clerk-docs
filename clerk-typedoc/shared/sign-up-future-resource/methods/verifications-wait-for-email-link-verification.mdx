### `verifications.waitForEmailLinkVerification()`

Will wait for email link verification to complete or expire after calling [`verifications.sendEmailLink()`](/docs/reference/objects/sign-up-future#verifications-send-email-link).

```typescript
function waitForEmailLinkVerification(): Promise<{ error: null | ClerkError }>
```
