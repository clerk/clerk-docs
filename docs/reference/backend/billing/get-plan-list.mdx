---
title: '`getPlanList()`'
description: Use the getPlanList() method to retrieve a list of Billing Plans.
---

<Include src="_partials/billing/billing-experimental" />

<Typedoc src="backend/billing-api/methods/get-plan-list" />

## Example

<Include src="_partials/backend/usage" />

```tsx
const { data, totalCount } = await clerkClient.billing.getPlanList({ payerType: 'org' })
```

## Backend API (BAPI) endpoint

This method in the SDK is a wrapper around the BAPI endpoint `GET /billing/plans`. See the [BAPI reference](/docs/reference/backend-api/tag/billing/GET/billing/plans){{ target: '_blank' }} for more information.
