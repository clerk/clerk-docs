**TLS (Transport Layer Security)** is a cryptographic protocol that encrypts data transmitted between two systems over a network, preventing eavesdropping and tampering. It is the standard mechanism for securing web traffic (HTTPS) and email delivery.
