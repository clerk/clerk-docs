---
title: 'Module'
description: The Clerk module provides session and user context to Clerk's composables and components.
---

The Clerk module is required to integrate Clerk into your Nuxt application, providing session and user context to Clerk's composables and components.

## Usage

To configure Clerk with Nuxt, you must add the module to your modules array in your `nuxt.config.ts` file.

The `clerkPlugin` accepts optional options, such as `{ signInForceRedirectUrl: '/dashboard' }`. See the [properties section](#properties) for more information.

```ts {{ filename: 'nuxt.config.ts' }}
export default defineNuxtConfig({
  modules: ['@clerk/nuxt'],
  clerk: {
    // It can accept options, such as { signInForceRedirectUrl: '/dashboard' }
  }
})
```

To update Clerk options at runtime, use the [`updateClerkOptions()`](/docs/references/vue/update-clerk-options) function.

<Typedoc src="clerk-react/clerk-provider-props" />
