Use `clerk.auth.setActive()` to switch the [Active Organization](!active-organization) for a session. Pass `sessionId` and `organizationId` to select an Organization, or omit `organizationId` (or pass `nil`) to select the user's [Personal Account](!personal-account). The latter isn't allowed when `clerk.environment?.organizationSettings.forceOrganizationSelection` is `true`.
