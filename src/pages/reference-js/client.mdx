import { Tab, Tabs } from "nextra-theme-docs";
import { Tables } from "@/components/Table";
import { Images } from "@/components/Images";
import { CodeBlockTabs } from "@/components/CodeBlockTabs";

# Client

The `Client` object keeps track of the authenticated sessions in the current device. The device can be a browser, a native application or any other medium that is usually the requesting part in a request/response architecture.

The `Client` object also holds information about any sign in or sign up attempts that might be in progress, tracking the sign in or sign up progress.

## Properties

<Tables
  headings={["Name", "Type", "Description"]}
  headingsMeta={[{}, {}, {maxWidth: "300px"}]}
  rows={[
{cells: [<code>signIn</code>, <code>SignIn | null</code>, <>The current sign in attempt, or null if there is none.</>]},
{cells: [<code>signUp</code>, <code>SignUp  | null</code>, <>The current sign up attempt, or null if there is none.</>]},
{cells: [<code>sessions</code>, <code><a href="/reference-js/session">Session</a>[]</code>, <>A list of sessions that have been created on this client.</>]},
{cells: [<code>activeSessions</code>, <code><a href="/reference-js/session">Session</a>[]</code>, <>A list of active sessions on this client.</>]},
{cells: [<code>lastActiveSessiond</code>, <code>string</code>, <>Unique identifier of the last active <a href="/reference-js/session">Session</a> on this client.</>]},
{cells: [<code>updatedAt</code>, <code>Date</code>, <>Timestamp of last update for the client.</>]},
  ]}
/>

## Methods

### `isNew()`

```typescript
function isNew(): boolean;
```

Returns **true** if this client hasn't been saved (created) yet in the [Frontend API](https://reference.clerk.dev/reference/frontend-api-reference), **false** otherwise.

### `create()`

```typescript
function create(): Promise<Client>;
```

Creates a new client for the current instance along with its cookie.

### `destroy()`

```typescript
function destroy(): Promise<void>;
```

Deletes the client. All sessions will be reset.
