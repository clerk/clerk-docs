<If sdk="nextjs">
  > [!IMPORTANT]
  > This guide assumes you've configured [`clerkMiddleware()`](/docs/reference/nextjs/clerk-middleware) in your Next.js application. The `auth()` helper requires Clerk Middleware to be configured before it can read request authentication state.
</If>

<If sdk="react-router">
  > [!IMPORTANT]
  > This guide assumes you've configured [`clerkMiddleware()`](/docs/reference/react-router/clerk-middleware) in your React Router application. The `getAuth()` helper requires Clerk Middleware to be configured before it can read request authentication state.
</If>

<If sdk="tanstack-react-start">
  > [!IMPORTANT]
  > This guide assumes you've configured [`clerkMiddleware()`](/docs/reference/tanstack-react-start/clerk-middleware) in your TanStack React Start application. The `auth()` helper requires Clerk Middleware to be configured before it can read request authentication state.
</If>
