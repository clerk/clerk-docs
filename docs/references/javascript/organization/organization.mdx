---
title: Organization
description: This is a class that represents an organziation. It's returned from the createOrganization and getOrganization methods on the Clerk class instance.
---

import { Tab, Tabs } from "nextra-theme-docs";
import { Tables } from "@/components/Table";
import { Images } from "@/components/Images";
import { CodeBlockTabs } from "@/components/CodeBlockTabs";

# Organization

This is a class that represents an organization. It's returned from the [`createOrganization`](/docs/references/javascript/clerk/create-organizaiton) and [`getOrganization`](/docs/references/javascript/clerk/organization-methods) methods on [the `Clerk` class instance](/docs/references/javascript/clerk/clerk).

## Properties

<Tables
  headings={["Name", "Type", "Description"]}
  headingsMeta={[{maxWidth:'300px'},{maxWidth: '300px'},{maxWidth: '300px'}]}
  rows={[
  {cells: [ <code>id</code>, <code>string</code>, "The unique identifier of the related organization."]},
  {cells: [ <code>name</code>, <code>string</code>, "The name of the related organization."]},
  {cells: [ <code>slug</code>, <code>string | null</code>, "The organization slug. If supplied, it must be unique for the instance."]},
  {cells: [ <code>logoUrl</code>, <code>string | null</code>, "The full URL for the organization logo. (deprecated in favor of imageUrl)"]},
  {cells: [ <code>imageUrl</code>, <code>string</code>, "Holds the organization logo or default logo"]},
  {cells: [ <code>membersCount</code>, <code>number</code>, "The number of members the associated organization contains"]},
  {cells: [ <code>pendingInvitationsCount</code>, <code>number</code>, "The number of pending invitations to users to join the organization."]},
  {cells: [ <code>publicMetadata</code>, <code>object</code>, <>
  Additional information about the organization that can be read through the <a href="https://reference.clerk.dev/reference/frontend-api-reference">Frontend API</a>, but written only from the <a href="https://clerk.com/docs/reference/backend-api">Backend API</a>.
  </>]},
  {cells: [ <code>createdAt</code>, <code>Date</code>, "Date of the time the organization was created."]},
  {cells: [ <code>updatedAt</code>, <code>Date</code>, "Date of the last time the user was updated."]},
  ]}
/>

## Methods

### `update()`

```typescript
function update(params: UpdateOrganizationParams): Promise<Organization>;
```

Updates an organization's attributes.

#### `UpdateOrganizationParams`

<Tables
  headings={["Name", "Type", "Description"]}
  headingsMeta={[{maxWidth:'300px'},{maxWidth: '300px'},{maxWidth: '300px'}]}
  rows={[
  {cells: [ <code>name</code>, <code>string</code>, "The organization name."]},
  {cells: [ <code>slug</code>, <code>string | undefined</code>, "The organization slug."]},
  ]}
/>

#### Returns

<Tables
  headings={["Type", "Description"]}
  headingsMeta={[{},{maxWidth: '300px'}]}
  rows={[
  {cells: [ <code>Promise{"<"}<a href=/docs/references/javascript/organization/organization">Organization</a>{">"}</code>, <>This method returns a <code>Promise</code> which resolves to an Organization.</>]},
  ]}
/>

### `destroy()`

```typescript
function destroy(): Promise<void>;
```

Deletes the organization. Only administrators can delete an organization.

Please note that deleting an organization will also delete all memberships and invitations. This is not reversible.

#### Returns

<Tables
  headings={["Type", "Description"]}
  headingsMeta={[{}, {maxWidth:'300px'}]}
  rows={[
  {cells: [ <code>{"Promise<void>"}</code>, <>This method returns a <code>Promise</code> which doesn't resolve to any value.</>]},
  ]}
/>

### `setLogo()`

```typescript
function setLogo(params: SetOrganizationLogoParams): Promise<Organization>;
```

Sets or replaces an organization's logo.  Accept the logo as a file or blob. The logo must be an image and its  size cannot exceed 10MB.

#### `SetOrganizationLogoParams`

<Tables
  headings={["Name", "Type", "Description"]}
  headingsMeta={[{maxWidth:'300px'},{maxWidth: '300px'},{maxWidth: '300px'}]}
  rows={[
  {cells: [ <code>file</code>, <code>File | Blob | null</code>, <>An image file which cannot exceed 10MB. Passing <code>null</code> will delete the organization's current logo.</>]},
  ]}
/>

#### Returns

<Tables
  headings={["Type", "Description"]}
  headingsMeta={[{}, {maxWidth:'300px'}]}
  rows={[
  {cells: [ <code>Promise{"<"}<a href="/docs/references/javascript/organization/organization">Organization</a>{">"}</code>, <>This method returns a <code>Promise</code> which resolves to an Organization.</>]},
  ]}
/>

## Additional methods

In addition to the methods listed above, the `Organization` class also has the following methods:

### Members

*   [`getMemberships()`](/docs/references/javascript/organization/members#get-memberships)
*   [`addMember()`](/docs/references/javascript/organization/members#add-member)
*   [`updateMember()`](/docs/references/javascript/organization/members#update-member)
*   [`removeMember()`](/docs/references/javascript/organization/members#remove-member)

### Invitations

*   [`getPendingInvitations()`](/docs/references/javascript/organization/invitations#get-pending-invitations)
*   [`inviteMember()`](/docs/references/javascript/organization/invitations#invite-member)
*   [`inviteMembers()`](/docs/references/javascript/organization/invitations#invite-members)
