> [!NOTE]
> This is a route module. It assumes you've registered [`clerkPlugin()`](/docs/reference/fastify/clerk-plugin) for your routes, which is what makes the request's authentication state available to `getAuth()`.
