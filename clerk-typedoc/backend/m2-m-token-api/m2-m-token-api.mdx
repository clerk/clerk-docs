## Constructors

### Constructor

#### Parameters

| Parameter       | Type                      | Description                                                                                                                                                                                            |
| --------------- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `request`       | `LegacyRequestFunction`   | -                                                                                                                                                                                                      |
| `verifyOptions` | `JwtMachineVerifyOptions` | JWT verification options (secretKey, apiUrl, etc.). Passed explicitly because BuildRequestOptions are captured inside the buildRequest closure and are not accessible from the RequestFunction itself. |

#### Returns

`M2MTokenApi`

#### Overrides

`AbstractAPI.constructor`
