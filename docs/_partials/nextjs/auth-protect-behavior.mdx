When a user isn't authenticated, [`auth.protect()`](/docs/reference/nextjs/app-router/auth#auth-protect) redirects them to the sign-in page for document requests (such as pages), returns a `401` for Server Actions, and returns a `404` for other non-document requests (such as Route Handlers).
