---
title: 'Integration'
description: The Astro integration provides session and user context to Clerk's hooks and components.
---

The Clerk integration is required to integrate Clerk into your Astro application, providing session and user context to Clerk's hooks, nanostores, and components.

## Usage

```ts {{ filename: 'astro.config.mjs' }}
import { defineConfig } from 'astro/config'
import clerk from '@clerk/astro'

export default defineConfig({
  integrations: [clerk()],
})
```

<Typedoc src="clerk-react/clerk-provider-props" />
