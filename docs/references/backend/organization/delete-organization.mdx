---
title: deleteOrganization()
description: Use Clerk's Backend SDK to delete an organization.
---

# `deleteOrganization()`

Deletes an [`Organization`](/docs/references/javascript/organization/organization) given a valid ID.

```tsx
function deleteOrganization: (organizationId: string) => Promise<Organization>;
```

## Parameters

| Name | Type | Description |
| --- | --- | --- |
| `organizationId` | `string` | The ID of the organization to delete. |

## Example

```tsx
const organizationId = 'org_2b6TtF4XlxDsKo6t0E0UyywGB72';

const response = await clerkClient.organizations.deleteOrganization(organizationId);

console.log(response);
/*
DeletedObject {
  object: 'organization',
  id: 'org_2b6TtF4XlxDsKo6t0E0UyywGB72',
  slug: null,
  deleted: true
}
*/
```

## Backend API (BAPI) endpoint

This method in the SDK is a wrapper around the BAPI endpoint `DELETE/organizations/{organization_id}`. See the [BAPI reference](https://clerk.com/docs/reference/backend-api/tag/Organizations#operation/DeleteOrganization) for more details.
