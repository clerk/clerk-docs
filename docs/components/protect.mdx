---
title: <Protect />
description: The `<Protect />` component is used to only render content that a user with a role with the correct permissions can access.
---

# `<Protect />`

The `<Protect />` component is used to render... TODO the rest is boilerplate from another component. Just follow the format.

## Usage

```jsx
<Protect role="org:admin">
    {children}
</Protect>
```

```jsx
<Protect permission="org:domains:read">
  {children}
</Protect>
```

<Tabs type="framework" items={["Next.js", "React", "Remix"]}>
  <Tab>
    ```jsx
    import { Protect } from "@clerk/nextjs";

    export default function ProtectPage() {
        return (
            <Protect role="org:admin">
                {children}
            </Protect>
        );
    }
    ```
  </Tab>

  <Tab>
    ```jsx
    import { Protect } from "@clerk/react";

    export default function ProtectPage() {
        return (
            <Protect role="org:admin">
                {children}
            </Protect>
        );
    }
    ```
  </Tab>
</Tabs>

## Properties

TODO: are these optional?

| Name | Type | Description |
| --- | --- | --- |
| `role` | `string` | TODO |
| `permission` | `string` | TODO. |