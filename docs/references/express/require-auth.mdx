---
description: Learn how to protect routes in your Express.js application using the `requireAuth()` middleware function.
---

# `requireAuth()`

`requireAuth()` is a middleware function that you can use to protect routes in your Express.js application. This function checks if the user is authenticated or authorized, and returns a 401 status code if they are not.

`clerkMiddleware()` is required to be set in the middleware chain before this util is used.

## Parameters

