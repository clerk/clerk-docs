import { Tab, Tabs } from "nextra-theme-docs";
import { Tables } from "@/components/Table";
import { Images } from "@/components/Images";

# `<UserProfile />`

<Images width={976} height={762} src="/images/ui-components/component-user_profile.svg" alt="UserProfile example"/>

The `<UserProfile/>` component is used to render a beautiful, full-featured account management UI that allows users to manage their profile and security settings. 

## Usage

Using the [Next.js optional catch all route](https://nextjs.org/docs/routing/dynamic-routes#optional-catch-all-routes), you can embed the `<UserProfile/>` component. This allows you to redirect the user inside your application and handle any redirects.

```jsx copy filename="/pages/user-profile/[[...index]].tsx"
import { UserProfile } from "@clerk/nextjs";

const UserProfilePage = () => (
  <UserProfile path="/user-profile" routing="path" />
);

export default UserProfilePage;
```

## Props

<Tables
  headings={["Name", "Type", "Description"]}
  rows={[
    {
      cells: ["appearance", "object", `Controls the overall look and feel.`],
    },
    {
      cells: [
        "routing",
        "RoutingStrategy",
        `The routing strategy for your pages. Supported values are:
-hash (default): Hash based routing.
-path: Path based routing.`,
      ],
    },
    {
      cells: [
        "path",
        "string",
        `string
The path where the component is mounted when path-based routing is used.
-e.g. /user-profile. This prop is ignored in hash-based routing.`,
      ],
    },
    {
      cells: [
        "hideNavigation",
        `boolean`,
        `Hides the default navigation bar. Can be used when a custom navigation bar is built.`,
      ],
    },
    {
      cells: [
        "only",
        `string`,
        `Renders only a specific page of the UserProfile component. Supported values are:
- account: User account page.
- security: User security page.`,
      ],
    },
  ]}
/>