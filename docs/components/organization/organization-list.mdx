---
title: <OrganizationList />
description: The `<OrganizationList />` component is used to display organization related memberships, invitations, and suggestions for the user.
---

# `<OrganizationList />`

<Images
  alt="Organization List Example"
  width={992}
  height={568}
  src="/docs/images/ui-components/component-org_list.svg"
/>

The `<OrganizationList />` component is used to display organization related memberships, invitations, and suggestions for the user.

## Usage

<Tabs items={['Next.js', 'React', 'Remix']}>
  <Tab>
    <CodeBlockTabs options={["App Router", "Pages Router"]}>
      ```jsx copy filename="/app/organization-list/[[...organization-list]]/page.[jsx/tsx]"
      import { OrganizationList } from "@clerk/nextjs";

      export default function OrganizationListPage() {
        return (
          <div>
            <OrganizationList />
          </div>
        );
      }
      ```

      ```jsx copy filename="/pages/organization-list/[[...index]].[jsx/tsx]"
      import { OrganizationList } from "@clerk/nextjs";

      export default function OrganizationListPage() {
        return (
          <div>
            <OrganizationList />
          </div>
        );
      }
      ```
    </CodeBlockTabs>
  </Tab>

  <Tab>
    ```jsx copy filename="organization-switcher.tsx"
    import { OrganizationList } from "@clerk/clerk-react";

    export default function OrganizationListPage() {
      return (
        <div>
          <OrganizationList />
        </div>
      );
    }
    ```
  </Tab>

  <Tab>
    ```jsx copy filename="/pages/organization-list/$.tsx"
    import { OrganizationList } from "@clerk/remix";

    export default function OrganizationListPage() {
      return (
        <div>
          <OrganizationList />
        </div>
      );
    }
    ```
  </Tab>
</Tabs>


## Props

All props below are optional.

TODO: add all props

<Tables
  headings={["Name", "Type", "Description"]}
  headingsMeta={[{maxWidth:'300px'},{maxWidth: '300px'},{maxWidth: '300px'}]}
  rows={[
    {
      cells: [
        <code>hidePersonal</code>,
        <code>boolean</code>,
        <>
          TODO
          <br/>Defaults to: <code>true</code>
        </>,
      ],
    },
  ]}
/>
