import { Tab, Tabs } from "nextra-theme-docs";
import { Callout } from "nextra-theme-docs";
import { Tables } from "@/components/Table";

# `<SignUpButton />`

`<SignUpButton/>` is a button that links to the sign-up page or displays the sign-up modal.

## Usage

<Tabs items={['Next.js', 'React', 'Remix', 'Gatsby']}>
<Tab>
```jsx copy filename="pages/index.js"
import { SignUpButton } from "@clerk/nextjs";

export default function Home() {
  return (
    <div>
    <h1> Sign up </h1>
      <SignUpButton />
    </div>
  );
}
```
You can also overide the default button with your own button and text.

```jsx copy filename="pages/index.js"
import { SignUpButton } from "@clerk/nextjs";

export default function Home() {
  return (
    <div>
      <h1> Sign up </h1>
      <SignUpButton mode="modal">
        <button className="btn">Sign up</button>
      </SignUpButton>
    </div>
  );
}
```

</Tab>
<Tab>
```jsx copy filename="example.js"
import { SignUpButton } from "@clerk/clerk-react";

export default function Example() {
  return (
    <div>
    <h1> Sign up </h1>
      <SignUpButton />
    </div>
  );
}
```
You can also overide the default button with your own button and text.
```jsx copy filename="example.js"
import { SignUpButton } from "@clerk/clerk-react";

export default function Example() {
  return (
    <div>
    <h1> Sign up </h1>
      <SignUpButton mode="modal">
        <button className="btn">
          Sign up
        </button>
      </SignUpButton>
    </div>
  );
}
```
</Tab>
<Tab>
```jsx copy filename="pages/index.js"
import { SignUpButton } from "@clerk/remix";

export default function Home() {
  return (
    <div>
    <h1> Sign up </h1>
      <SignUpButton />
    </div>
  );
}
```
You can also overide the default button with your own button and text.
```jsx copy filename="pages/index.js"
import { SignUpButton } from "@clerk/remix";

export default function Home() {
  return (
    <div>
    <h1> Sign up </h1>
      <SignUpButton mode="modal">
        <button className="btn">
          Sign up
        </button>
      </SignUpButton>
    </div>
  );
}
```
</Tab>
<Tab>
```jsx copy filename="pages/index.js"
import { SignUpButton } from "gatsby-plugin-clerk";

export default function Home() {
  return (
    <div>
    <h1> Sign up </h1>
      <SignUpButton />
    </div>
  );
}
```
You can also overide the default button with your own button and text.
```jsx copy filename="pages/index.js"
import { SignUpButton } from "gatsby-plugin-clerk";

export default function Home() {
  return (
    <div>
    <h1> Sign up </h1>
      <SignUpButton mode="modal">
        <button className="btn">
          Sign up
        </button>
      </SignUpButton>
    </div>
  );
}
```
</Tab>
</Tabs

>
