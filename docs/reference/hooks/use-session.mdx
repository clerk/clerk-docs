---
title: useSession()
description: Access and manage the current user's session in your React application with Clerk's useSession() hook.
sdk: chrome-extension, expo, nextjs, react, react-router, tanstack-react-start
---

The `useSession()` hook provides access to the current user's [`Session`](/docs/reference/objects/session) object, as well as helpers for setting the active session.

<Typedoc src="react/use-session-return" />

## Example

### Access the `Session` object

<Include src="_partials/hooks/use-session" />
