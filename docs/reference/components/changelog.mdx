---
title: Component Changelog
description: Keep track of the changes to Clerk's components.
---

Clerk's components are updated regularly to improve the user experience and add new features. For most users, these updates will be rolled out automatically. Use this changelog to learn about changes and new features being released. If you have [specified a specific Clerk UI version](/docs/reference/components/versioning), use this changelog to understand how new releases might impact your customizations.

## 1.12.0 (2026-05-18)

### Minor Changes {{ toc: false }}

- Add `autoFocus` appearance option to disable automatic input focusing.

### Patch Changes {{ toc: false }}

- Improve Floating UI usage: fix `ariaLabel` typo in `MenuTrigger`, replace imperative floating ref in `MenuList` with `useMergeRefs`, remove manual position offset in `SelectOptionList`, add `aria-haspopup` to `MenuTrigger`, and add missing ARIA attributes to `Select` components.
- Add support for custom SAML provider in `<ConfigureSSO />`.
- Update `NavBar` to receive `containerSx` prop.

## 1.11.0 (2026-05-15)

### Minor Changes {{ toc: false }}

- Add `highlightedPlan` prop to PricingTable default layout to render a "Popular" badge on matching plans.
- Add support for inline `<bold>` markup in localization values, rendered as `<strong>` elements.

### Patch Changes {{ toc: false }}

- Add two-mode segmented control to SAML config submission sub-step in `<__experimental_ConfigureSSO />`.
- Add confirmation step for `<__experimental_ConfigureSSO />`.
- Add test step for `<__experimental_ConfigureSSO />`.

## 1.10.0 (2026-05-14)

### Minor Changes {{ toc: false }}

- Add `fontFamilyMono` appearance variable for customizing monospace font used in Clerk components.

### Patch Changes {{ toc: false }}

- Implement Okta SAML metadata URL submission path in Configure step of `<__experimental_ConfigureSSO />`.
- Implement provider selection step of `<__experimental_ConfigureSSO />`.
- Update `<ConfigureSSO />` to only allow managing enterprise connections based on system permission.
- Fix: don't treat numeric usernames as phone numbers.
- Fixed custom page icons not rendering in React 19.
- Add verify/add email address step to `<__experimental_ConfigureSSO />`.
- Refactor `<__experimental_ConfigureSSO />` into layered primitive set.

## 1.9.1 (2026-05-08)

### Patch Changes {{ toc: false }}

- Fixed unhandled TypeError when `unsafeMetadata` is passed to `<SignUp />`.

## 1.9.0 (2026-05-07)

### Minor Changes {{ toc: false }}

- Removed unused internal OAuthConsent prop.

### Patch Changes {{ toc: false }}

- Add wizard steps for `<__experimental_ConfigureSSO />` component.
- Remove back button on sign-in password compromised/pwned error screen.

## 1.8.0 (2026-05-05)

### Minor Changes {{ toc: false }}

- Introduce experimental `<ConfigureSSO />` component for SSO setup (not production-ready).

### Patch Changes {{ toc: false }}

- Localize API keys table headers.
- Surface initialization errors and stalled mounts in component renderer with console logging.

## 1.7.0 (2026-05-01)

### Minor Changes {{ toc: false }}

- Render OAuthConsent organization selector from `user:org:read` scope.
- Expose `OAuthConsent` as public component export across React-based SDKs.

## 1.6.6 (2026-04-24)

### Patch Changes {{ toc: false }}

- Display web3wallet in UserProfile when added by administrator.

## 1.6.5 (2026-04-23)

### Patch Changes {{ toc: false }}

- Fixed OAuth `redirect_url` for `openSignIn` modal.

## 1.6.4 (2026-04-22)

### Patch Changes {{ toc: false }}

- Default organization selection in `<OAuthConsent />` to user's last active organization.

## 1.6.3 (2026-04-17)

### Patch Changes {{ toc: false }}

- Fix EnableOrganizationsPrompt in keyless mode showing correct CTA.
- Use pre-loaded `user.organizationMemberships` to populate org select.
- Correctly display IP redirect URIs in OAuth consent.
- Add scroll-driven fade overlays to ListGroupContent in OAuthConsent.

## 1.6.2 (2026-04-16)

### Patch Changes {{ toc: false }}

- Add infinite loading to organization selection in `<OAuthConsent />`.
- Fix OAuthConsent always redirecting to sign-in using AuthenticatedRoutes pattern.

## 1.6.0 (2026-04-14)

### Minor Changes {{ toc: false }}

- Introduce internal `<OAuthConsent />` component for zero-config OAuth consent screen.

## 1.5.0 (2026-04-08)

### Minor Changes {{ toc: false }}

- Add support for rendering Banned badge in organization members list.

## 1.4.0 (2026-04-07)

### Minor Changes {{ toc: false }}

- API keys now generally available with `<APIKeys />` component and `useAPIKeys()` hook.

### Patch Changes {{ toc: false }}

- Adjust padding and display logo on `OrganizationList` header.

## 1.3.1 (2026-04-01)

### Patch Changes {{ toc: false }}

- Display enterprise connection icon on account linking dropdown in UserProfile.

## 1.3.0 (2026-04-01)

### Minor Changes {{ toc: false }}

- Add support for seat-based billing plans in Clerk Billing.
- Add `EnterpriseConnection` resource (`User.getEnterpriseConnections()` now returns correct type).
- Allow linking external accounts to enterprise accounts via `UserProfile`.

### Patch Changes {{ toc: false }}

- Improve error handling when creating API keys.
- Use distinct password placeholder for sign-up forms.

## 1.2.4 (2026-03-25)

### Patch Changes {{ toc: false }}

- Move `react` and `react-dom` from dependencies to peerDependencies.
- Fix false positive in structural CSS detection for `.cl-internal-*` classes.

## 1.2.3 (2026-03-20)

### Patch Changes {{ toc: false }}

- Fix issue where default plans offered ability to switch to annual.

## 1.2.2 (2026-03-17)

### Patch Changes {{ toc: false }}

- Improve spacing for `CreateOrganization` and `OrganizationList`.
- Fix issue where monthly/annual toggle appeared on default free plans.

## 1.2.0 (2026-03-10)

### Minor Changes {{ toc: false }}

- Add support for annual-only Billing plans.

## 1.1.0 (2026-03-09)

### Minor Changes {{ toc: false }}

- Prevent modification of immutable attributes in UserProfile.
- Remove remotely hosted code from new `@clerk/ui` package.

## 1.0.1 (2026-03-04)

### Patch Changes {{ toc: false }}

- Update `MIN_CLERK_JS_VERSION` to `6.0.0` for Core 3 release.

## 1.0.0 (2026-03-03)

_Initial release of the versioned components._
