---
title: <Gate />
description: The `<Gate />` component is used to only render content that a user with a role with the correct permissions can access.
---

# `<Gate />`

The `<Gate />` component is used to render... TODO the rest is boilerplate from another component. Just follow the format.

## Usage

```jsx
<Gate role="org:admin">
    {children}
</Gate>
```

```jsx
<Gate permission="org:domains:read">
  {children}
</Gate>
```

<Tabs type="framework" items={["Next.js", "React", "Remix"]}>
  <Tab>
    ```jsx
    import { Gate } from "@clerk/nextjs";

    export default function GatePage() {
        return (
            <Gate role="org:admin">
                {children}
            </Gate>
        );
    }
    ```
  </Tab>

  <Tab>
    ```jsx
    import { Gate } from "@clerk/react";

    export default function GatePage() {
        return (
            <Gate role="org:admin">
                {children}
            </Gate>
        );
    }
    ```
  </Tab>
</Tabs>

## Properties

TODO: are these optional?

| Name | Type | Description |
| --- | --- | --- |
| `role` | `string` | TODO |
| `permission` | `string` | TODO. |