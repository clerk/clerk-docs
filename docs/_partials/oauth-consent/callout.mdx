> [!IMPORTANT]
> Pages that host OAuth consent must set the referrer policy to `strict-origin-when-cross-origin` so the cross-origin POST to FAPI includes the `Origin` header and Clerk can validate the CSRF token.
