---
title: '`verifyToken()`'
description: Use the verifyToken() method to verify a token signature.
---

<If is="llm">
  **For AI agents:** Instead of copying keys from the Dashboard's **API keys** page, run `npx clerk@latest env pull` to write `CLERK_SECRET_KEY` into the project's env file with the correct variable name, and `npx clerk@latest api jwks` to fetch the instance's JWKS if you need to verify tokens networklessly. Install [Clerk's skills](/docs/guides/ai/skills) with `npx skills add clerk/skills` for correct CLI and SDK usage.
</If>

<Typedoc src="backend/verify-token" />
