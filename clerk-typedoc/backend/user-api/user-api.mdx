## banUser()

Marks the given [`User`](/docs/reference/backend/types/backend-user) as banned, which means that all their sessions are revoked and they are not allowed to sign in again.

### Parameters

| Parameter | Type     | Description                |
| --------- | -------- | -------------------------- |
| `userId`  | `string` | The ID of the user to ban. |

### Returns

`Promise`\<[`User`](../user.mdx)\>

---

## createUser()

Creates a [`User`](/docs/reference/backend/types/backend-user) in your instance.

Your settings in the [Clerk Dashboard](https://dashboard.clerk.com) determine how you should setup your user model. Anything **Required** will need to be provided when creating a user. Trying to add a field that isn't enabled will result in an error.

Any email address and phone number created using this method will be automatically verified.

> [!CAUTION]
>
> This endpoint is [rate limited](/docs/guides/how-clerk-works/system-limits). For development instances, a rate limit rule of **100 requests per 10 seconds** is applied.
> For production instances, that limit goes up to **1000 requests per 10 seconds**.

### Parameters

| Parameter | Type                                            |
| --------- | ----------------------------------------------- |
| `params`  | [`CreateUserParams`](../create-user-params.mdx) |

### Returns

`Promise`\<[`User`](../user.mdx)\>

---

## deleteUser()

Deletes the given [`User`](/docs/reference/backend/types/backend-user).

### Parameters

| Parameter | Type     | Description                   |
| --------- | -------- | ----------------------------- |
| `userId`  | `string` | The ID of the user to delete. |

### Returns

`Promise`\<[`User`](../user.mdx)\>

---

## deleteUserBackupCodes()

Deletes all backup codes for the given user.

### Parameters

| Parameter | Type     | Description                                    |
| --------- | -------- | ---------------------------------------------- |
| `userId`  | `string` | The ID of the user to delete backup codes for. |

### Returns

`Promise`\<`UserID`\>

---

## deleteUserExternalAccount()

Deletes an external account for the given user.

### Parameters

| Parameter | Type                                                                            |
| --------- | ------------------------------------------------------------------------------- |
| `params`  | [`DeleteUserExternalAccountParams`](../delete-user-external-account-params.mdx) |

### Returns

`Promise`\<[`DeletedObject`](../deleted-object.mdx)\> — The [`DeletedObject`](/docs/reference/backend/types/deleted-object) object.

---

## deleteUserPasskey()

Deletes the passkey identification for a given user and notifies them through email.

### Parameters

| Parameter | Type                                                           |
| --------- | -------------------------------------------------------------- |
| `params`  | [`DeleteUserPasskeyParams`](../delete-user-passkey-params.mdx) |

### Returns

`Promise`\<[`DeletedObject`](../deleted-object.mdx)\> — The [`DeletedObject`](/docs/reference/backend/types/deleted-object) object.

---

## deleteUserProfileImage()

Deletes a user's profile image.

### Parameters

| Parameter | Type     | Description                                         |
| --------- | -------- | --------------------------------------------------- |
| `userId`  | `string` | The ID of the user to delete the profile image for. |

### Returns

`Promise`\<[`User`](../user.mdx)\> — The updated [`User`](/docs/reference/backend/types/backend-user).

---

## deleteUserTOTP()

Deletes all of the TOTP secrets for the given user.

### Parameters

| Parameter | Type     | Description                                        |
| --------- | -------- | -------------------------------------------------- |
| `userId`  | `string` | The ID of the user to delete the TOTP secrets for. |

### Returns

`Promise`\<`UserID`\>

---

## deleteUserWeb3Wallet()

Deletes a Web3 wallet identification for the given user.

### Parameters

| Parameter | Type                                                         |
| --------- | ------------------------------------------------------------ |
| `params`  | [`DeleteWeb3WalletParams`](../delete-web3-wallet-params.mdx) |

### Returns

`Promise`\<[`DeletedObject`](../deleted-object.mdx)\> — The [`DeletedObject`](/docs/reference/backend/types/deleted-object) object.

---

## disableUserMFA()

Disable all of a user's MFA methods (e.g. [OTP](!otp) sent via SMS, TOTP on their authenticator app) at once.

### Parameters

| Parameter | Type     | Description                            |
| --------- | -------- | -------------------------------------- |
| `userId`  | `string` | The ID of the user to disable MFA for. |

### Returns

`Promise`\<`UserID`\>

---

## getCount()

Gets the total number of users in your instance.

### Parameters

| Parameter | Type                                          |
| --------- | --------------------------------------------- |
| `params`  | [`UserCountParams`](../user-count-params.mdx) |

### Returns

`Promise`\<`number`\>

---

## getOrganizationInvitationList()

Gets a list of the given user's Organization invitations. By default, the list is returned in descending order by creation date (newest first).

### Parameters

| Parameter        | Type                                                                                      | Description                                                                                                                                                                            |
| ---------------- | ----------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `params`         | [`GetOrganizationInvitationListParams`](../get-organization-invitation-list-params-1.mdx) | -                                                                                                                                                                                      |
| `params.limit?`  | `number`                                                                                  | Maximum number of items returned per request. Must be an integer greater than zero and less than `501`. Can be used for paginating the results together with offset. Defaults to `10`. |
| `params.offset?` | `number`                                                                                  | Skip the first `offset` items when paginating. Needs to be an integer greater or equal to zero. To be used in conjunction with `limit`. Defaults to `0`.                               |
| `params.status?` | <code>"pending" \| "accepted" \| "revoked" \| "expired"</code>                            | Filters the invitations by the provided status.                                                                                                                                        |
| `params.userId`  | `string`                                                                                  | The ID of the user to get the list of Organization invitations for.                                                                                                                    |

### Returns

`Promise`\<[`PaginatedResourceResponse`](../paginated-resource-response.mdx)\<<code>[OrganizationInvitation](/docs/reference/backend/types/backend-invitation)[]</code>\>\> — A [`PaginatedResourceResponse`](/docs/reference/backend/types/paginated-resource-response) object with a `data` property that contains an array of [`OrganizationInvitation`](/docs/reference/backend/types/backend-organization-invitation) objects, and a `totalCount` property that indicates the total number of Organization invitations for the user.

---

## getOrganizationMembershipList()

Gets a list of the given user's Organization memberships. By default, the list is returned in descending order by creation date (newest first).

### Parameters

| Parameter        | Type                                                                                      | Description                                                                                                                                                                            |
| ---------------- | ----------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `params`         | [`GetOrganizationMembershipListParams`](../get-organization-membership-list-params-1.mdx) | -                                                                                                                                                                                      |
| `params.limit?`  | `number`                                                                                  | Maximum number of items returned per request. Must be an integer greater than zero and less than `501`. Can be used for paginating the results together with offset. Defaults to `10`. |
| `params.offset?` | `number`                                                                                  | Skip the first `offset` items when paginating. Needs to be an integer greater or equal to zero. To be used in conjunction with `limit`. Defaults to `0`.                               |
| `params.userId`  | `string`                                                                                  | The ID of the user to get the list of Organization memberships for.                                                                                                                    |

### Returns

`Promise`\<[`PaginatedResourceResponse`](../paginated-resource-response.mdx)\<<code>[OrganizationMembership](../organization-membership.mdx)[]</code>\>\> — A [`PaginatedResourceResponse`](/docs/reference/backend/types/paginated-resource-response) object with a `data` property that contains an array of [`OrganizationMembership`](/docs/reference/backend/types/backend-organization-membership) objects, and a `totalCount` property that indicates the total number of Organization memberships for the user.

---

## getUser()

Gets a [`User`](/docs/reference/backend/types/backend-user) for the specified user ID.

### Parameters

| Parameter | Type     | Description                     |
| --------- | -------- | ------------------------------- |
| `userId`  | `string` | The ID of the user to retrieve. |

### Returns

`Promise`\<[`User`](../user.mdx)\>

---

## getUserList()

Retrieves the list of users in your instance. By default, the list is returned in descending order by creation date (newest first).

### Parameters

| Parameter                      | Type                                                                                                                                                                                               | Description                                                                                                                                                                            |
| ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `params`                       | [`UserListParams`](../user-list-params.mdx)                                                                                                                                                        | -                                                                                                                                                                                      |
| `params.createdAtAfter?`       | `number`                                                                                                                                                                                           | Filters users who were created after the given date (with millisecond precision).                                                                                                      |
| `params.createdAtBefore?`      | `number`                                                                                                                                                                                           | Filters users who were created before the given date (with millisecond precision).                                                                                                     |
| `params.emailAddress?`         | <code>string[]</code>                                                                                                                                                                              | Filters users with the specified email addresses. Accepts up to 100 email addresses.                                                                                                   |
| `params.externalId?`           | <code>string[]</code>                                                                                                                                                                              | Filters users with the specified external IDs. Accepts up to 100 external IDs.                                                                                                         |
| `params.last_active_at_since?` | `number`                                                                                                                                                                                           | **Deprecated.** Use `lastActiveAtAfter` instead. This parameter will be removed in a future version.                                                                                   |
| `params.lastActiveAtAfter?`    | `number`                                                                                                                                                                                           | Filters users who were last active after the given date (with millisecond precision).                                                                                                  |
| `params.lastActiveAtBefore?`   | `number`                                                                                                                                                                                           | Filters users who were last active before the given date (with millisecond precision).                                                                                                 |
| `params.lastSignInAtAfter?`    | `number`                                                                                                                                                                                           | Filters users who were last signed in after the given date (with millisecond precision).                                                                                               |
| `params.lastSignInAtBefore?`   | `number`                                                                                                                                                                                           | Filters users who were last signed in before the given date (with millisecond precision).                                                                                              |
| `params.limit?`                | `number`                                                                                                                                                                                           | Maximum number of items returned per request. Must be an integer greater than zero and less than `501`. Can be used for paginating the results together with offset. Defaults to `10`. |
| `params.offset?`               | `number`                                                                                                                                                                                           | Skip the first `offset` items when paginating. Needs to be an integer greater or equal to zero. To be used in conjunction with `limit`. Defaults to `0`.                               |
| `params.orderBy?`              | `WithSign`\<<code>"created_at" \| "updated_at" \| "email_address" \| "web3wallet" \| "first_name" \| "last_name" \| "phone_number" \| "username" \| "last_active_at" \| "last_sign_in_at"</code>\> | Filters users in a particular order. Prefix a value with `+` to sort in ascending order, or `-` to sort in descending order. Defaults to `-created_at`.                                |
| `params.organizationId?`       | <code>string[]</code>                                                                                                                                                                              | Filters users who are members of the specified Organizations. Accepts up to 100 Organization IDs.                                                                                      |
| `params.phoneNumber?`          | <code>string[]</code>                                                                                                                                                                              | Filters users with the specified phone numbers. Accepts up to 100 phone numbers.                                                                                                       |
| `params.query?`                | `string`                                                                                                                                                                                           | Filters users matching the given query across email addresses, phone numbers, usernames, Web3 wallet addresses, user IDs, first names, and last names. Partial matches supported.      |
| `params.userId?`               | <code>string[]</code>                                                                                                                                                                              | Filters users with the specified user IDs. Accepts up to 100 user IDs.                                                                                                                 |
| `params.username?`             | <code>string[]</code>                                                                                                                                                                              | Filters users with the specified usernames. Accepts up to 100 usernames.                                                                                                               |
| `params.web3Wallet?`           | <code>string[]</code>                                                                                                                                                                              | Filters users with the specified Web3 wallet addresses. Accepts up to 100 Web3 wallet addresses.                                                                                       |

### Returns

`Promise`\<[`PaginatedResourceResponse`](../paginated-resource-response.mdx)\<<code>[User](../user.mdx)[]</code>\>\> — A [`PaginatedResourceResponse`](/docs/reference/backend/types/paginated-resource-response) object with a `data` property that contains an array of [`User`](/docs/reference/backend/types/backend-user) objects, and a `totalCount` property that indicates the total number of users in your instance.

---

## getUserOauthAccessToken()

### Call Signature

Gets the corresponding [OAuth access token](!oauth-access-token) for a user that has previously authenticated with the given OAuth provider.

#### Parameters

| Parameter  | Type                                                         | Description                                                                                                                                              |
| ---------- | ------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `userId`   | `string`                                                     | The ID of the user to get the OAuth access tokens for.                                                                                                   |
| `provider` | [`OAuthProvider`](/docs/reference/types/sso#o-auth-provider) | The OAuth provider to get the access tokens for. If using a custom OAuth provider, prefix the provider name with `custom_` (e.g. `oauth_custom_google`). |

#### Returns

`Promise`\<[`PaginatedResourceResponse`](../paginated-resource-response.mdx)\<<code>[OauthAccessToken](../oauth-access-token.mdx)[]</code>\>\> — A [`PaginatedResourceResponse`](/docs/reference/backend/types/paginated-resource-response) object with a `data` property that contains an array of [`OauthAccessToken`](/docs/reference/backend/types/backend-oauth-access-token) objects, and a `totalCount` property that indicates the total number of OAuth access tokens for the specified user and provider.

### Call Signature

#### Parameters

| Parameter  | Type                                                                    |
| ---------- | ----------------------------------------------------------------------- |
| `userId`   | `string`                                                                |
| `provider` | <code>[OAuthStrategy](/docs/reference/types/sso#o-auth-strategy)</code> |

#### Returns

`Promise`\<[`PaginatedResourceResponse`](../paginated-resource-response.mdx)\<<code>[OauthAccessToken](../oauth-access-token.mdx)[]</code>\>\>

#### Deprecated

Use `getUserOauthAccessToken` without the `oauth_` provider prefix .

---

## lockUser()

Locks the given [`User`](/docs/reference/backend/types/backend-user), which means that they are not allowed to sign in again until the lock expires or is manually unlocked. By default, lockout duration is 1 hour, but it can be configured in the application's [**Attack protection**](https://dashboard.clerk.com/~/protect/attack-protection) settings. See the [guide on user locks](/docs/guides/secure/user-lockout).

### Parameters

| Parameter | Type     | Description                 |
| --------- | -------- | --------------------------- |
| `userId`  | `string` | The ID of the user to lock. |

### Returns

`Promise`\<[`User`](../user.mdx)\>

---

## replaceUserMetadata()

Replaces the metadata associated with the specified user. Unlike [`updateUserMetadata()`](/docs/reference/backend/user/update-user-metadata), which deep-merges into the existing metadata, this method uses replace semantics: when a metadata field is provided, its previous value is overwritten in full with no merging at any level.

The distinction is at two layers:

- **Top-level field omission preserves the existing value.** Each top-level field (`publicMetadata`, `privateMetadata`, `unsafeMetadata`) is handled independently. If you don't include a field in the request, the stored value for that field is left untouched.
- **The value inside a provided field is replaced in full.** When you do include a field, its previous content is discarded — any nested keys present before but absent in the new value are dropped. There is no merge.

For the provided field, you can also send:

- `{}` (empty object) to clear the field.
- `null` to overwrite the field with a JSON `null` value. Prefer `{}` unless you specifically need a stored `null`.

### Parameters

| Parameter                 | Type                                                                                                                                                                                                                                                                                                      | Description                                                                                                                                                    |
| ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `userId`                  | `string`                                                                                                                                                                                                                                                                                                  | The ID of the user to replace the metadata for.                                                                                                                |
| `params`                  | <code>\{ privateMetadata?: [UserPrivateMetadata](/docs/reference/types/metadata#user-private-metadata); publicMetadata?: [UserPublicMetadata](/docs/reference/types/metadata#user-public-metadata); unsafeMetadata?: [UserUnsafeMetadata](/docs/reference/types/metadata#user-unsafe-metadata); \}</code> | The metadata to replace.                                                                                                                                       |
| `params.privateMetadata?` | [UserPrivateMetadata](/docs/reference/types/metadata#user-private-metadata)                                                                                                                                                                                                                               | Metadata that can be read and set only from the [Backend API](/docs/reference/backend-api){{ target: '_blank' }}.                                              |
| `params.publicMetadata?`  | [UserPublicMetadata](/docs/reference/types/metadata#user-public-metadata)                                                                                                                                                                                                                                 | Metadata that can be read from the Frontend API and [Backend API](/docs/reference/backend-api){{ target: '_blank' }} and can be set only from the Backend API. |
| `params.unsafeMetadata?`  | [UserUnsafeMetadata](/docs/reference/types/metadata#user-unsafe-metadata)                                                                                                                                                                                                                                 | Metadata that can be read and set from the Frontend API. It's considered unsafe because it can be modified from the Frontend API.                              |

### Returns

`Promise`\<[`User`](../user.mdx)\> — The updated [`User`](/docs/reference/backend/types/backend-user).

---

## setPasswordCompromised()

Sets the given user's password as compromised. The user will be prompted to reset their password on their next sign-in. See the [guide on password protection and rules](/docs/guides/secure/password-protection-and-rules#reject-compromised-passwords) for more information.

### Parameters

| Parameter                   | Type                                            | Description                                                      |
| --------------------------- | ----------------------------------------------- | ---------------------------------------------------------------- |
| `userId`                    | `string`                                        | The ID of the user to set the password as compromised for.       |
| `params`                    | <code>\{ revokeAllSessions?: boolean; \}</code> | Other parameters for the request.                                |
| `params.revokeAllSessions?` | `boolean`                                       | Whether to revoke all sessions of the user. Defaults to `false`. |

### Returns

`Promise`\<[`User`](../user.mdx)\> — The updated [`User`](/docs/reference/backend/types/backend-user).

---

## unbanUser()

Removes the ban mark from the given [`User`](/docs/reference/backend/types/backend-user), allowing them to sign in again.

### Parameters

| Parameter | Type     | Description                  |
| --------- | -------- | ---------------------------- |
| `userId`  | `string` | The ID of the user to unban. |

### Returns

`Promise`\<[`User`](../user.mdx)\>

---

## unlockUser()

Removes a sign-in lock from the given [`User`](/docs/reference/backend/types/backend-user), allowing them to sign in again. See the [guide on user locks](/docs/guides/secure/user-lockout).

### Parameters

| Parameter | Type     | Description                   |
| --------- | -------- | ----------------------------- |
| `userId`  | `string` | The ID of the user to unlock. |

### Returns

`Promise`\<[`User`](../user.mdx)\>

---

## unsetPasswordCompromised()

Unsets the given user's password as compromised. The user will no longer be prompted to reset their password on their next sign-in. See the [guide on password protection and rules](/docs/guides/secure/password-protection-and-rules#reject-compromised-passwords) for more information.

### Parameters

| Parameter | Type     | Description                                                  |
| --------- | -------- | ------------------------------------------------------------ |
| `userId`  | `string` | The ID of the user to unset the password as compromised for. |

### Returns

`Promise`\<[`User`](../user.mdx)\> — The updated [`User`](/docs/reference/backend/types/backend-user).

---

## updateUser()

Updates the given [`User`](/docs/reference/backend/types/backend-user).

### Parameters

| Parameter | Type                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Description                    |
| --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ |
| `userId`  | `string`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | The ID of the user to update.  |
| `params`  | <code>\{ backupCodes?: string[]; createdAt?: Date; createOrganizationEnabled?: boolean; createOrganizationsLimit?: number; deleteSelfEnabled?: boolean; externalId?: string; firstName?: string; lastName?: string; legalAcceptedAt?: Date; locale?: string; notifyPrimaryEmailAddressChanged?: boolean; password?: string; primaryEmailAddressID?: string; primaryPhoneNumberID?: string; primaryWeb3WalletID?: string; privateMetadata?: [UserPrivateMetadata](/docs/reference/types/metadata#user-private-metadata); profileImageID?: string; publicMetadata?: [UserPublicMetadata](/docs/reference/types/metadata#user-public-metadata); signOutOfOtherSessions?: boolean; skipLegalChecks?: boolean; skipPasswordChecks?: boolean; totpSecret?: string; unsafeMetadata?: [UserUnsafeMetadata](/docs/reference/types/metadata#user-unsafe-metadata); username?: string; \} & object \| \{ backupCodes?: string[]; createdAt?: Date; createOrganizationEnabled?: boolean; createOrganizationsLimit?: number; deleteSelfEnabled?: boolean; externalId?: string; firstName?: string; lastName?: string; legalAcceptedAt?: Date; locale?: string; notifyPrimaryEmailAddressChanged?: boolean; password?: string; primaryEmailAddressID?: string; primaryPhoneNumberID?: string; primaryWeb3WalletID?: string; privateMetadata?: [UserPrivateMetadata](/docs/reference/types/metadata#user-private-metadata); profileImageID?: string; publicMetadata?: [UserPublicMetadata](/docs/reference/types/metadata#user-public-metadata); signOutOfOtherSessions?: boolean; skipLegalChecks?: boolean; skipPasswordChecks?: boolean; totpSecret?: string; unsafeMetadata?: [UserUnsafeMetadata](/docs/reference/types/metadata#user-unsafe-metadata); username?: string; \} & \{ passwordDigest: string; passwordHasher: "argon2i" \| "argon2id" \| "awscognito" \| "bcrypt" \| "bcrypt_sha256_django" \| "md5" \| "pbkdf2_sha256" \| "pbkdf2_sha256_django" \| "pbkdf2_sha1" \| "phpass" \| "scrypt_firebase" \| "scrypt_werkzeug" \| "sha256" \| "md5_phpass" \| "ldap_ssha"; \}</code> | The user attributes to update. |

### Returns

`Promise`\<[`User`](../user.mdx)\>

---

## updateUserMetadata()

Updates the metadata for the given user, by merging existing values with the provided parameters.

A "deep" merge will be performed - "deep" means that any nested JSON objects will be merged as well. You can remove metadata keys at any level by setting their value to `null`.

> [!TIP]
> If you want to fully replace the existing metadata instead of merging, use [`replaceUserMetadata()`](/docs/reference/backend/user/replace-user-metadata).

### Parameters

| Parameter                 | Type                                                                                                                                                                                                                                                                                                      | Description                                                                                                                                                    |
| ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `userId`                  | `string`                                                                                                                                                                                                                                                                                                  | The ID of the user to update.                                                                                                                                  |
| `params`                  | <code>\{ privateMetadata?: [UserPrivateMetadata](/docs/reference/types/metadata#user-private-metadata); publicMetadata?: [UserPublicMetadata](/docs/reference/types/metadata#user-public-metadata); unsafeMetadata?: [UserUnsafeMetadata](/docs/reference/types/metadata#user-unsafe-metadata); \}</code> | The metadata to update.                                                                                                                                        |
| `params.privateMetadata?` | [UserPrivateMetadata](/docs/reference/types/metadata#user-private-metadata)                                                                                                                                                                                                                               | Metadata that can be read and set only from the [Backend API](/docs/reference/backend-api){{ target: '_blank' }}.                                              |
| `params.publicMetadata?`  | [UserPublicMetadata](/docs/reference/types/metadata#user-public-metadata)                                                                                                                                                                                                                                 | Metadata that can be read from the Frontend API and [Backend API](/docs/reference/backend-api){{ target: '_blank' }} and can be set only from the Backend API. |
| `params.unsafeMetadata?`  | [UserUnsafeMetadata](/docs/reference/types/metadata#user-unsafe-metadata)                                                                                                                                                                                                                                 | Metadata that can be read and set from the Frontend API. It's considered unsafe because it can be modified from the Frontend API.                              |

### Returns

`Promise`\<[`User`](../user.mdx)\> — The updated [`User`](/docs/reference/backend/types/backend-user).

---

## updateUserProfileImage()

Updates the profile image for the given user. To remove the profile image, see [`deleteUserProfileImage()`](/docs/reference/backend/user/delete-user-profile-image).

### Parameters

| Parameter | Type                                   | Description                                         |
| --------- | -------------------------------------- | --------------------------------------------------- |
| `userId`  | `string`                               | The ID of the user to update the profile image for. |
| `params`  | <code>\{ file: Blob \| File; \}</code> | The file to set as the user's profile image.        |

### Returns

`Promise`\<[`User`](../user.mdx)\> — The updated [`User`](/docs/reference/backend/types/backend-user).

---

## verifyPassword()

Check that the user's password matches the supplied input. Useful for custom auth flows and re-verification.

### Parameters

| Parameter | Type                                                    |
| --------- | ------------------------------------------------------- |
| `params`  | [`VerifyPasswordParams`](../verify-password-params.mdx) |

### Returns

`Promise`\<<code>\{ verified: true; \}</code>\>

---

## verifyTOTP()

Verify that the provided TOTP or backup code is valid for the user. Verifying a backup code will result it in being consumed (i.e. it will become invalid). Useful for custom auth flows and re-verification.

### Parameters

| Parameter | Type                                            |
| --------- | ----------------------------------------------- |
| `params`  | [`VerifyTOTPParams`](../verify-totp-params.mdx) |

### Returns

`Promise`\<<code>\{ code_type: "totp"; verified: true; \}</code>\>
