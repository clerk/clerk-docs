> [!NOTE]
> This guide uses the Core 2 `useSignIn()` and `useSignUp()` hooks available in Core 3 SDKs via the `/legacy` subpath. If you're using a Core 2 SDK, remove the `/legacy` subpath from the import path.
